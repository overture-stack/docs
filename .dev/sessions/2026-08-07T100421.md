Removed phase 0 from the rebuild plan and ran phase 1, ahead of stage 3 rather than after it. Nothing pushed.

---

**Phase 0 is gone.** The three Matomo goals, their `[NEEDS: go-ahead, and Matomo access]` input, and the open question about defining them now are all out of `roadmap.md`. Nothing else referenced them.

**Phase 1 was gated on stage 3, and the gate was satisfied rather than ignored.** Both `roadmap.md` and `ia-proposal.md` held phase 1 back because it edits the pages stage 2 compares against the live Gatsby site, which would leave the port unverifiable. What was actually at risk was a *before* snapshot, and the live Gatsby site is not going anywhere, so the fix was to capture one: nine routes at 390px, 900px and 1440px from a production build, in `.dev/baseline-stage2/`, before anything changed. Stage 2 now compares that against production whenever it runs. The roadmap records the reordering and the mitigation together.

That capture also found two horizontal overflows the earlier screenshot pass had reported clean, on `/acknowledgements/` at 390px and `/getting-started/` at 1440px. Both routes retired an hour later, so neither was worth fixing.

---

**Funder content is off the developer site.** `/about/funding/` carries all five grants and the additional-support list; `/impact/publications/` carries the paper, the author list and four citation formats. Both `docs/community-docs/` originals are pointers now. The retired `/acknowledgements/` stylesheet was renamed rather than replaced, since the funding content wants exactly its hero, header and two-column detail layout.

The pointers link root-relative, which resolves while the marketing pages and the documentation are one build and breaks the moment stage 3 splits them. That break is deliberate: `onBrokenLinks: "throw"` fails the docs build at exactly the point the links need to become absolute, which is a better reminder than a comment nobody reads.

**`/getting-started/` retired, and the home page's copy of it went too.** Every Get Started CTA points at the Prelude docs. The home page repeated the same setup steps as the retired page, including the same dead `git clone -b quickstart`, and repeating them is what let both drift; it now orients and hands off instead. Checked the current commands against `deploy-docs/01-prelude.md` rather than carrying the old ones forward.

**`data/metrics.ts` is the single source for published figures.** ICGC-ARGO corrected to 100,000+ participants across 27 programs in 15 countries, plus 3,500+ registered users, against the 63,116 donors across 26 programs in 13 countries the site had been showing. VirusSeq rewritten as iMicroSeq around pathogen genomes and wastewater surveillance.

Two things about that file are worth keeping. Its `source` field names the platform or program that owns each number, not whichever document last quoted it, because a document name ages faster than the team does and this file compiles into the public bundle, so it must not name unannounced material. And the three figures with no traceable source are published with `verified: null` rather than quietly restated as current, so `findStaleMetrics()` returns them the moment the build gate has a caller for it.

**Institutional attribution.** One line under the home hero, and the About page now opens with who builds Overture ahead of the values grid, describing the team as software engineers rather than a taxonomy of roles. No navbar mark: the two planning documents disagreed there, and the proposal's § Decided rules a chrome lockup explicitly out, so the roadmap bullet asking for one was the stale half.

**The docs navbar links back to overture.bio**, absolute, which is correct both before and after stage 3.

---

**Verified**, against a served production build: typecheck clean, build clean apart from the known JSX-anchor false positive, all nine routes at three viewports with no horizontal overflow, and the two retired routes gone from the output. Screenshots reviewed rather than counted, which caught two things a clean build did not: both new heroes ran their subtitle under the hero graphic at 1440px, and the attribution line was washing out at 85% opacity against a WCAG 2.1 AA obligation. Shortened the subtitles and dropped the opacity; smaller type is what makes attribution subtle, not lower contrast.

**Three gaps are published and blocked on assets.** iMicroSeq still wears VirusSeq's logo and portal screenshot, because dropping them renders a broken image in the home carousel. The ICGC-ARGO component list still names Ego, which is discontinued, left alone because nothing sourced says what replaced it there. And the home page still describes the team as a taxonomy of roles in the band under the carousel, which belongs to the phase 4 reorder. All three are in Known issues with the assets they wait on.
