Pruned intermediate screenshot captures from `.dev/`, now that the rebuild is past the phase they documented.

- `.dev/after-phase1/`, `.dev/after-phase2/`, `.dev/after-phase3/`, `.dev/.DS_Store`: deleted. `baseline-stage2/` (the irreplaceable pre-rebuild capture) and `after-phase4/` (the current state) were kept.
- `.dev/roadmap.md`: updated the Working documents section to stop describing the deleted directories and note they aren't recoverable (`.dev/` is gitignored).

Replaced the home hero's DNA-helix background with `HeroDiagram`, an interactive orbit of Overture's components built from the supplied `diagram.pptx`, so the page's one piece of artwork stops implying Overture is genomics-only. Icon positions match the pptx's real coordinates (an evenly-spaced alternative was tried and reverted: the developer preferred the source artwork's own alignment). This is a distinct asset from the labeled Collect/Explore/Control diagram `roadmap.md` § Inputs needed still wants for `/products/`: the pptx carries no text, and the icon-to-component pairing is a content decision confirmed with the developer, including two pure codename puns (the tag with a music note is Lyric, the book on its stand is Lectern) that a functional reading of the icons would have missed. Also reworded the hero's second CTA, and found and fixed several real layout/accessibility bugs along the way.

- `website/src/marketing/data/heroDiagram.ts`: new. Layout (position, size, rotation) for the 8 hotspots plus the pairing to `data/components.ts`, so name/codename/summary stay a single source of truth with `/products/`. The 8th, Control, is not one of the seven components: an unshipped component confirmed with the developer, rendered with a "Coming soon" tooltip and no link until documentation exists (`[NEEDS: documentation URL]` in `roadmap.md`).
- `website/src/marketing/components/HeroDiagram.tsx`, `styles/components/_hero-diagram.scss`: new. Each hotspot is a real focusable/hoverable element (a `Link` to `/products/#<id>`, or a disabled `span` for Control), showing a tooltip on hover or focus. Hidden below `tablet-up` with `display: none` specifically so it drops out of the tab order where the artwork never renders, not just visually.
- `website/src/marketing/components/ProductGroup.tsx`: `/products/#lyric` and its six siblings existed in the rendered HTML but were never registered with Docusaurus's own anchor tracking (`useBrokenLinks().collectAnchor`), because that registration is normally automatic for MDX headings and list items and this page predates using either. Nothing linked to these anchors internally until `HeroDiagram`, so the gap was invisible until now. Fixed generically, for `group.id` as well as each `component.id`, rather than only the ones this change happens to use.
- `website/src/marketing/styles/pages/_home.scss`: hero layout is real flex now (`justify-content: space-between`), not a background image positioned by hand. Replaces two things that CSS background-position was papering over rather than fixing: the hero paragraph and the artwork overlapped by up to 158px across roughly 768-1216px viewport width (present with the old DNA texture too, invisible against a diffuse pattern, glaring against discrete icons), and the artwork sat top-aligned in a hero box that grows with the heading rather than centered in it. Verified zero overlap and equal top/bottom clearance at 12 widths from 768 to 1996.
- Two bugs specific to `HeroDiagram` itself, both caught by actually hovering the built page rather than reading the CSS: the tooltip's name line was invisible, dark-navy text on a dark-navy background, because `_defaults.scss` sets `strong { color: ... !important }` site-wide and inheriting white loses to that regardless of selector specificity; and an open tooltip could sit under a later hotspot in the same absolutely-positioned layer, fixed by raising the active hotspot's own `z-index` on hover/focus rather than just the tooltip's.
- `website/src/pages/home/index.tsx`: hero's second button is now "Learn More" linking to `/products/`, replacing "View Demo". `DEMO_LINK` (`website/src/marketing/constants/externalLinks.ts`) removed as a result: nothing else referenced it.

`HeroDiagram` grew a second view (who runs Overture, not just what it's made of) and went through two different shapes before landing on the one that shipped. Sequence, since both earlier ones left real code behind that a git log alone wouldn't explain:

1. First built as a hover reveal: hovering the center portal widened the two background rings and animated 8 platform logos out from behind it, plus a stats tooltip. Shipped, then reverted, on realizing hover has no equivalent on touch and is easy to miss even with a mouse: a control that only some visitors can find isn't "shown," so a dedicated carousel-review pass would very likely have flagged this before it shipped. That version is not in the repo; its zone-hover math (a 130%-oversized hit-region so the pointer could travel from the portal to a logo without the reveal collapsing mid-transit) doesn't need to survive anywhere for the record.
2. Considered and rejected: a horizontal-scrolling carousel below the hero for the same content. `roadmap.md`'s own history was the reason: phase 4 explicitly removed a case-study carousel, reasoning that a visitor saw one card at a time and hid the rest behind a click. A second carousel would reintroduce exactly that tradeoff this repo already argued its way out of once.
3. Shipped, then superseded same day: a two-slide, dot-indexed carousel *inside* the existing circular frame, not a new section. Slide 1 was the component-icon ring; slide 2 was 8 platform/partner logos around the Overture wordmark ("Powered by Overture") plus the proof band's own three figures as a caption, two dots below the diagram switching between them.
4. What actually shipped: the developer reviewed slide 2 against the proof band immediately below it and pointed out the two were now saying the same thing twice in a row (the same three figures, mostly the same logos). Rather than trim one to fit next to the other, "who runs Overture" moved out of the diagram entirely: `HeroDiagram` is back to a single, always-visible view (component icons only, no dots, no second slide), and a new component, `LogoCarousel`, carries the platform/partner logos as a continuously auto-scrolling marquee in its own section right below the hero, replacing the proof band's static logo row rather than sitting alongside it. The proof band keeps its three figures; nothing on the page shows logos and figures twice anymore.

- `website/src/marketing/data/heroDiagram.ts`: back to hotspot data only. `HeroDiagramLogo`, `platformLogo()`, `heroDiagramLogos` and `heroDiagramProjectStats` moved out (see `partnerLogos.ts` below) or dropped outright (the caption stats, now redundant with the proof band a scroll away).
- `website/src/marketing/data/partnerLogos.ts`: new. The same 8 platforms/partners slide 2 had (ICGC-ARGO, ICGC 25K, HCMI, Kids First, EU CanCan, PCGL, OHCRN, iMicroSeq), as a flat list rather than ring coordinates: `LogoCarousel` flows them, it doesn't position them individually. Same href fallback as the rest of the page, a platform's own page if it has one, `/impact/` if not; EU CanCan still has no entry in `platforms.ts`, `lineage`, or `adopters`, so it still renders with no link, `[NEEDS: context]` in `roadmap.md`.
- `website/src/marketing/components/LogoCarousel.tsx`, `styles/components/_logo-carousel.scss`: new. The list renders twice back to back and the whole track animates left by exactly one list's width on a loop, so the seam is invisible; the second copy is `aria-hidden` and every one of its links is `tabIndex={-1}`, existing only to make that loop seamless, never as content to tab into or hear twice. Pauses on `:hover` and `:focus-within`, so a keyboard user tabbing into a logo gets the same still frame a mouse user gets by pointing at one. Under `prefers-reduced-motion` the animation and the duplicate list both drop out, leaving the same static, wrapped row the proof band used to show. The section's own background is the hero's `$blue-top`, and the left/right edge fades are a gradient to that same colour, not a new one: the point is that this reads as a continuation of the hero, not a new section starting.
- `website/src/pages/home/index.tsx`: `<LogoCarousel />` renders right after `.Hero`; the proof band (`.HomeProof`) keeps its `<dl>` of three figures and loses its `<ul>` of logos, now `LogoCarousel`'s job alone.
- `website/src/marketing/styles/pages/_home.scss`: `.HomeProof` (the proof band) pulled up closer to the hero. `.section`'s 3rem plus the page's generic `.container` padding stacked to roughly 128px of blank space between the hero and "Running in production since 2016", reading as a gap rather than the next thing on the page. Still true with `LogoCarousel` sitting between them.
- `website/src/marketing/data/platforms.ts`: real logos for `icgcargo`, `humanCancerModels`, `virusseq`, `ohcrn`, `pcgl`, and the lineage entries `icgc25k` and `kidsFirst` (added a `logo` field to `LineagePlatform`, previously only on `Platform`). `ohcrn` and `pcgl` were both `[NEEDS: logo files]` in `roadmap.md`; both are now closed. `icgcargo`, `humanCancerModels`, and `virusseq`'s logos were generated placeholder text badges, not real marks (same discovery that already covered `virusseq`'s wrong VirusSeq branding), confirmed with the developer before replacing all three so the same platform reads consistently everywhere it appears, not just inside `LogoCarousel`.

Same day, a second round on `LogoCarousel` itself, from developer feedback after seeing it built: bigger logos, a slower scroll a visitor can take over rather than just watch, and the edge fades tied to the hero's own content edges rather than an independent measurement that happened to look right at one width.

- `website/src/marketing/data/platforms.ts`, `partnerLogos.ts`: real logos for `ihcc` (replacing an existing, non-placeholder logo with an updated one the developer supplied) and the `gdc` lineage entry (previously had none). `LogoCarousel` grew from 8 to 10 logos: `ihcc` was already real but had been left out of the original slide-2 set, apparently by oversight; `gdc` simply couldn't be shown before now.
- `website/src/marketing/components/LogoCarousel.tsx`: rebuilt the scroll mechanics from a CSS `transform` animation to a `requestAnimationFrame` loop against a real scroll container (`overflow-x: auto`), so a visitor can take over with a trackpad swipe, shift+wheel, or a mouse drag (the one browsers don't do for you, handled here with pointer events) without fighting the animation. Hovering or focusing anything inside stops the loop outright, not just visually, so a resumed animation never fights a visitor's own scroll position. First version of this loop wrote its per-frame increment straight to `scrollLeft` and read it back next frame; at this speed each frame's move is under a pixel, and `scrollLeft` is integer-quantized, so it rounded back to zero every single frame and the carousel never actually moved, silently. Fixed by keeping the real position in a float local to the effect and only writing the rounded result to the DOM.
- `website/src/marketing/styles/components/_logo-carousel.scss`: logos bigger (60px max-height, up from 40; chips 92px tall, up from 64) and the scroll speed slower, both direct developer feedback after seeing the first version.
- `website/src/marketing/styles/pages/_home.scss`: added `.LogoCarousel .container { padding: 0 32px; }`, matching `.Hero .container`'s own override right above it. Without this, `LogoCarousel`'s edge fades lined up with the generic `.container` padding (an unrelated rule, further out), not with where the hero text and diagram actually sit; a first attempt at fixing this by putting the override inside `_logo-carousel.scss` itself compiled to the same specificity as `.HomePage .container`'s own padding override and lost the tie on source order, so it silently had no effect until moved here, next to the rule it needs to match.

Third pass, from developer testing on a real machine rather than anything caught here first: a repaint bug in the edge fades, five logos shown fully with a sixth peeking rather than however many happened to fit, and tighter hero padding.

- `website/src/marketing/styles/components/_logo-carousel.scss`: the edge fades (`&__fade`, was `&__viewport`'s `::before`/`::after`) moved to real elements, siblings of the scrolling viewport rather than pseudo-elements inside it. Screenshots showed a logo rendering half-faded, split cleanly down the middle, while scrolling: Chrome leaves stale paint behind absolutely-positioned descendants of an element whose `scrollLeft` is being driven from JS rather than moved by the user's own input, which is exactly what this carousel does every frame. This is a known class of Chromium compositing bug, not anything specific to how this carousel was built; living outside the scrolled element entirely sidesteps it rather than chasing the specific missing repaint. Not reproduced in headless Chrome even under a deliberate stress test (repeated drags back and forth across the loop's wrap point) either before or after the fix, so the fix is verified by removing the entire class of cause, not by seeing the bug and watching it go away.
- `website/src/marketing/styles/components/_logo-carousel.scss`: chips down from 92px to 80px tall, logos down from 60px/190px to 52px/150px, gap down from 56px to 36px: five logos now sit fully inside the container at the common desktop width with a sixth peeking at the edge, the developer's actual target, rather than however many happened to fit at a size picked in isolation.
- `website/src/marketing/styles/pages/_home.scss`: `.Hero`'s own vertical padding down from 3.8em to 2.2em and `min-height` down from 470px to 380px; `.Hero__section`'s desktop-up padding changed from `50px` (all sides) to `0 50px`. That second one was the biggest single contributor: 50px of padding above and below the hero text, stacked on top of `.Hero`'s own vertical padding on the outer box, doing most of the "empty space" the developer was seeing.

Still open, not attempted: hovering a component in `HeroDiagram` highlighting the platforms in `LogoCarousel` that use it. This needs a platform-to-component usage mapping that doesn't exist anywhere on record (not in `platforms.ts`, not in `components.ts`); `[NEEDS: mapping]` in `roadmap.md` rather than guessed at, since it's a factual claim about real deployments.

Fourth pass: a production date under each `LogoCarousel` logo, editable by the developer, and the logos reordered presently-deployed-first.

- `website/src/marketing/data/partnerLogos.ts`: `PartnerLogo` grew `date?: string` and `current: boolean`. For the six presently-deployed platforms, `date` defaults to `` `Since ${platform.launched}` `` — `launched` already existed on `Platform` (from `metrics.ts`, already shown elsewhere on the page in Featured Deployments), so this is the same figure restated, not new data. PCGL's `date` is an explicit override, `"In development"`: `metrics.ts` deliberately carries no public launch date for it (another institution's to announce), so this states that rather than standing in for a real date. The three lineage logos (ICGC 25K, GDC, Kids First) and EU CanCan get no `date` at all: there's no record of when any of them ran, and showing nothing is the honest option next to guessing a range. `[NEEDS: dates]` in `roadmap.md`.
- `website/src/marketing/data/partnerLogos.ts`: reordered to presently-deployed first (oldest `launched` to newest, per the same field), then PCGL (in development, not yet running but not past either), then the three lineage logos in `platforms.ts`'s existing order, then EU CanCan last (status unconfirmed either way, not sorted into a bucket it might not belong in).
- `website/src/marketing/components/LogoCarousel.tsx`, `styles/components/_logo-carousel.scss`: each `<li>` is now a small flex column, the logo chip and (if present) a `LogoCarousel__date` caption underneath; absent for the four logos with no date rather than rendering an empty line. The date is also folded into the link's `title` (`"iMicroSeq — Since 2021"`), so it reaches a screen reader or a mouse tooltip the same way the name alone did before.

Real dates for three of the four logos that had none, from the developer: ICGC 25K ran 2007-2024; Overture's components were first used at GDC in 2016 and at Kids First in 2018 (both no longer current, hence no end date implied by "since"). `data/partnerLogos.ts`: those three now show a bare year or range ("2016", "2007-2024") rather than "Since Y" — deliberately, since "since" reads as still-ongoing and these aren't. EU CanCan remains the one logo with neither a confirmed relationship nor a date (`[NEEDS: context]` in `roadmap.md`).

Also asked, separately: fill the logo chips more, crop and center rather than leaving dead space. First attempt used a fixed box (`object-fit: cover`) so every logo would render at an identical size — screenshotted before calling it done, per the usual rule for anything visual, and it was a real regression: `cover` crops whatever doesn't match the box's aspect ratio, which sliced text-based wordmark logos in half ("Human Cancer Models Initiative" rendered as "man Cancer / odels Initiati", ARGO's full name cut mid-word). Reverted to a fixed height with the width left to each logo's own aspect ratio (`object-fit: contain`, no forced width): fills the chip vertically with no gap above or below, never crops, and the chip (no fixed width of its own already) just wraps a wordmark logo a little wider instead of padding it with empty margin. Screenshotted again to confirm: no cropped text, five logos still fit fully in the common desktop width.

Fifth pass: EU CanCan removed (the developer's own call, not a resolution of its status — it never had a confirmed relationship to Overture, a date, or a component mapping), and the ordering rule replaced outright: in-development first, then most recent start year to oldest, across all nine logos as one list rather than a current-bucket/past-bucket split.

- `website/src/marketing/data/partnerLogos.ts`: `current: boolean` retired in favour of `startYear?: number`, used only for sorting (the display text is still the separate `date` string). The array is sorted with a comparator — entries with no `startYear` (only PCGL, in development) first, then descending — rather than hand-ordered, so a future date change reorders itself instead of needing the array reshuffled by hand.

Then: use `caseStudies.tsx` (the "How was Overture used?" list on each platform's `/impact/` page) to find which `HeroDiagram` components each `LogoCarousel` platform actually uses, and build the hover-to-highlight interaction the developer asked for after the second pass. Five of the nine had a documented list already: ICGC-ARGO (Song, Score, Maestro, Arranger), iMicroSeq (Score, Song, Maestro, Arranger, Lectern), Kids First (Song, Score, Arranger), IHCC (Arranger), HCMI (Arranger). OHCRN, PCGL, ICGC 25K and GDC have no such list anywhere in the codebase; `[NEEDS: mapping]` in `roadmap.md` rather than guessed at, same reasoning as every other factual gap this session.

- `website/src/marketing/data/componentUsage.ts`: new. The mapping above, `Record<string, string[]>` keyed by the same platform ids used everywhere else. ICGC-ARGO's case study also names a generic "access and authorization" capability; excluded, since `caseStudies.tsx`'s own comment says it names the capability rather than a component because Ego (what used to provide it) is discontinued and nothing sourced says what replaced it — it isn't Control, the new unshipped component, and mapping it there would claim a live component does something it doesn't yet.
- `website/src/marketing/context/ComponentHighlightContext.tsx`: new. A small context (`highlighted: string | null`, `setHighlighted`) shared between `HeroDiagram` and `LogoCarousel`, wrapped around both in `pages/home/index.tsx`. Outside the provider it no-ops rather than throwing, since highlighting is an enhancement neither component depends on to render.
- `website/src/marketing/components/HeroDiagram.tsx`: every hotspot now calls `setHighlighted(hotspot.id)` on hover/focus, `null` on leave/blur. Real bug, not theoretical: wired up with `onMouseEnter`/`onMouseLeave` first, and hovering did nothing — focus worked, hover didn't. Read `@docusaurus/core`'s `Link.js` to find out why rather than guessing: for the react-router branch (which is what a same-page anchor like `/products/#lyric` actually takes), it renders `<LinkComponent {...props} onMouseEnter={onInteractionEnter} ... />` — its own `onMouseEnter` (for hover-preload) is set *after* spreading the caller's props, so it silently wins regardless of what's passed in. `onMouseLeave`, `onFocus` and `onBlur` aren't touched, which is exactly the pattern that showed up (leave/focus/blur fired, enter didn't). Fixed by switching to `onPointerEnter`/`onPointerLeave`, which `Link.js` never references at all, rather than working around Docusaurus's Link internals directly.
- `website/src/marketing/components/LogoCarousel.tsx`, `styles/components/_logo-carousel.scss`: each item gets `--highlighted` (yellow ring, slight scale-up) or `--dimmed` (opacity 0.35) once something is highlighted, computed from `componentUsage`. The auto-scroll loop also treats an active highlight as a pause condition, same as hovering the carousel itself, so a platform that lights up doesn't scroll out of view mid-look. A hint line above the row ("Hover a component in the diagram above to see who uses it") is the discoverability answer to "make sure the interaction is intuitive that it exists": hidden below `tablet-up`, matching exactly when the interaction itself is possible (`HeroDiagram` is `display: none` there too), rather than promising something a phone visitor can't do.
- Verified with puppeteer, not just by reading the code: hovering "Search (Arranger)" highlights exactly the five platforms mapped to it and dims the rest; hovering "Dictionary Manager (Lectern)" highlights only iMicroSeq; the auto-scroll's `scrollLeft` sits still for over a second while a highlight is active and resumes the instant the pointer leaves; keyboard focus (`.focus()`) produces the same highlight as hover, confirming the fix didn't just move the bug from mouse to keyboard.

Two more from the developer, from actually using it: dimming in place isn't enough when the matches aren't on screen to dim, and the highlighted card's top edge was getting cut off.

- **Highlighting in place wasn't the fix, filtering was.** Dragging the marquee to an arbitrary scroll position and then hovering "Arranger" showed the highlight doing nothing visible, because none of its five matches happened to be scrolled into view — dimming five off-screen logos and five on-screen ones looks identical to a visitor who can only see the second five. `LogoCarousel.tsx` now swaps the marquee for a static, centered list of just the matches while a highlight is active, laid over the same space: `visibility: hidden` on the marquee (not unmounted), so its scroll position and the running `requestAnimationFrame` loop survive the swap instead of resetting every time. `--dimmed` is gone; nothing needs dimming when only matches are shown.
- **The clipped top edge**, confirmed from a screenshot: the highlight used `transform: scale(1.06)`, and `transform` doesn't affect layout size, so `&__viewport`'s `overflow-y: hidden` clipped whatever the scale grew past the original (unscaled) box. `_logo-carousel.scss`: replaced with a `border: 3px solid transparent` at rest, coloured on `--highlighted` — same box size either way, so there's nothing for `overflow-y: hidden` to clip. Padding dropped from 12px to 10px to keep the same ~52px of room for the logo itself now that the border eats into the same fixed-height box.
- Filtering to matches introduced a second layout bug, caught by screenshot before calling it done: five matches don't fit on one line, so the filtered list wraps to two rows, taller than the marquee it's laid over. `position: absolute; inset: 0` sized the filtered view to the marquee's fixed single-row height regardless, so the second row overlapped the hint text above it. Fixed by making `&__stage` a grid and giving the marquee viewport and the filtered view the same `grid-area: 1 / 1`: the row sizes to whichever is actually taller, rather than a size fixed to whichever one happens to render first.

The other direction, asked for next: hovering or focusing a platform in `LogoCarousel` highlights, back in `HeroDiagram`, the components that platform uses.

- `context/ComponentHighlightContext.tsx`: split into two independent values, `highlightedComponent`/`setHighlightedComponent` (hero → carousel, already existed, just renamed for clarity now that there are two) and `highlightedPlatform`/`setHighlightedPlatform` (carousel → hero, new). Both live in the same context since both are shared between the same two siblings, but they don't interact: each side only ever reads the axis the other side writes.
- `LogoCarousel.tsx`: every `LogoItem` calls `setHighlightedPlatform(logo.id)` on hover/focus, `null` on leave/blur — Pointer Events again, not `onMouseEnter`, same `@docusaurus/Link` bug as HeroDiagram's hotspots. This applies in both the marquee and the filtered view, since it's the same component either way.
- `HeroDiagram.tsx`: reads `highlightedPlatform`, looks up `componentUsage[highlightedPlatform]` and marks each hotspot `--highlighted` (a match) or `--dimmed` (not, while a platform is highlighted). Unlike the carousel's own logos, every hotspot is already on screen simultaneously — there's no "off-screen match" problem here, so dimming in place is enough; nothing needed filtering out.
- `_hero-diagram.scss`: `--highlighted` reuses the existing hover scale-up plus a yellow `drop-shadow` glow (safe here specifically because `.HeroDiagram` has no `overflow: hidden` anywhere above it, unlike the carousel viewport that clipped a transform earlier in this same session); `--dimmed` is a plain opacity drop.
- Verified with puppeteer: hovering the iMicroSeq logo highlights exactly the six components its case study names (Lyric, Song, Score, Maestro, Arranger, Lectern) and dims the other two (Control, Stage); keyboard focus on the ICGC-ARGO card produces the same four-component highlight as hovering it, and clears fully on blur; the original hero-to-carousel direction still filters correctly afterward, confirming the two axes don't interfere with each other.

The developer closed out the mapping and dates in three follow-up messages, plus asked for a new tooltip and a sizing fix.

- `data/componentUsage.ts`: the last four platforms mapped (OHCRN: Arranger; PCGL: Song, Score, Lectern, Lyric; ICGC 25K: Song, Score, Arranger, Maestro, Stage; GDC: Maestro, Arranger, Stage), plus Stage added to the five already mapped from `caseStudies.tsx` (ICGC-ARGO, IHCC, HCMI) — all nine platforms now have a confirmed list, closing the `[NEEDS: mapping]` this was blocked on. Also: Control, still in development, is being built for iMicroSeq and OHCRN specifically — the one entry in this file mapping a platform to a component before that component has shipped, since `HeroDiagram`'s own highlight logic doesn't care whether a component is live, only whether it's used.
- `data/partnerLogos.ts`: GDC and Kids First's dates changed from bare years ("2016", "2018") to "Originally 2016" / "Originally 2018". Both used Overture components in their first renditions, but the developer says both diverged enough since — still largely the same stack, heavily customized — that neither is considered "an Overture component" today; "Since" would have overstated that (implies still-current), and a bare year had no verb to qualify it correctly either way. ICGC 25K's "2007-2024" wasn't touched: a closed range doesn't have the same ambiguity a single year does.

A tooltip for LogoCarousel, "similar to the products tooltip", carrying a per-project impact statement.

- `data/partnerLogos.ts`: `PartnerLogo` gained `impact?: string`. Empty for all nine right now — this is a mechanism, not content the developer asked me to write, and every other data gap this session got the same treatment: nothing invented, `[NEEDS: impact statements]` in `roadmap.md` instead.
- `components/LogoCarousel.tsx`, `_logo-carousel.scss`: can't reuse HeroDiagram's tooltip approach outright — a plain `position: absolute` child popping up from the hovered logo would be clipped by `&__viewport`'s `overflow-y: hidden`, the same class of bug the highlight ring's `transform: scale` hit earlier in this file's history, for the same underlying reason. Rendered instead as a single shared `position: fixed` element, positioned in JS from the hovered item's own `getBoundingClientRect()`: `fixed` is positioned against the viewport itself, so no ancestor's `overflow` touches it. A small unexported context (`LogoTooltipContext`) carries the setter down to whichever `LogoItem` is hovered, in either the marquee or the filtered view, without adding a prop `LogoList` has to forward. Verified by temporarily setting a test string on one logo, screenshotting it rendered correctly positioned and fully on screen, then removing the test string before treating this as done — the mechanism is real, the content isn't, on purpose.
- The logo's `title` attribute is now conditional: present only for a logo with no `impact` yet, so a visitor never gets the plain browser tooltip and the rich one stacked on top of each other a second apart.

Last: two things caught from using the shipped hover-highlight interaction, not from reading the code.

- **More matches than fit on one line wrapped to two, and the developer asked to shrink the chips instead.** Hovering "Search (Arranger)" pulls in eight of the nine platforms now that OHCRN's mapped; at the filtered view's normal chip size, only four fit across the container's width before wrapping. `LogoCarousel.tsx` adds a `LogoCarousel__list--compact` class when `matches.length > 4`; `_logo-carousel.scss` shrinks the chip height, logo height, and gaps under it. First attempt shrank too aggressively on the developer's read; a second, close to it, cropped back to two rows at eight matches (checked directly, not by eye: distinct row `top` values via `getBoundingClientRect()`, not just a screenshot). Settled on a size in between, checked the same way at both the eight-match case and a five-match case, one row either way.

Last for this pass: three more requests in quick succession — tighten the hero/carousel seam further, a discoverability overlay with arrows, then a course correction on both.

- `_home.scss`, `_logo-carousel.scss`: `.Hero`'s tablet-up padding down to 1.4em (`min-height` 360px) and `.LogoCarousel`'s own top padding down to 20px, tightening the seam a second time.
- Built, then removed: a floating pill (`LogoCarousel__hintBadge`) straddling that same seam, with two curved SVG arrows meant to point up into the hero and down into the carousel. The down arrow never rendered visibly — confirmed with `elementFromPoint` and forced debug colours that it existed, was positioned correctly, and had valid stroke/fill, but a card in the marquee still painted over it, most likely a stacking-context interaction between the badge's own `z-index: 5` and the grid/positioned layers underneath that wasn't worth fully root-causing once the developer asked to drop the arrow idea entirely and focus elsewhere. The badge itself (no arrows) briefly replaced the old inline hint text, then came out too, on a direct "remove the badge overlay" — the seam is bare now, no hint element at all, just the tightened padding.
- **A real, separate bug, found the same way**: "make sure the tooltips don't cover up the diagram." `HeroDiagram`'s per-hotspot tooltip alignment (`align`/`vertical`, computed from the hotspot's own position) had the horizontal and vertical conditions backwards — every tooltip was pointed *toward* the ring's center (over the portal and the other icons) rather than away from it. Fixed by swapping both ternaries: a left-side hotspot now throws its tooltip further left (`end`, extending away from center) instead of right, and a top-half hotspot points up instead of down, matching what a bottom-half one already did in the opposite direction. Verified by hovering all eight hotspots and screenshotting each: no tooltip overlaps the portal or another icon anymore.
- The impact-tooltip content itself: on request, `data/partnerLogos.ts` now defaults `impact` to existing site copy rather than leaving it empty — a case study's own summary paragraph (`caseStudies.tsx`'s `listItems[2][0]`, the exact field that field's own type comment says was written for "the home page carousel," a different, long-retired one) where a platform has one, else the shorter `platform`/`lineage` `summary` used elsewhere on `/impact/`. This is reused, already-published copy, not new text invented for this — `[NEEDS: impact statements]` in `roadmap.md` still tracks replacing it with something purpose-built. Verified all nine tooltips render real text, not "undefined": three (ICGC 25K, GDC, Kids First) briefly looked broken in testing because the marquee had auto-scrolled them out of the browser's viewport bounds between test steps, not because of an actual bug — confirmed by pausing the marquee before checking, at which point all nine resolved correctly.

Two real screenshots from the developer's own machine — not this session's own puppeteer, an actual local `docusaurus start`, tabs and bookmarks bar and all — showed both tooltips still had real problems the "point away from center" fix and the filtered-view rework hadn't touched: `HeroDiagram`'s Lyric tooltip ran off the top of the browser window entirely (cut off mid-sentence by the window chrome, not by any element on the page), and `LogoCarousel`'s ICGC-ARGO tooltip, popping upward as designed, covered a chunk of the hero diagram sitting right above the carousel. The developer's own read: "below for the projects and within the window for the products" — two different fixes, not one.

- `utils/floatingTooltip.ts`: new, shared by both. `floatingTooltipPosition(rect, preferredSide, opts)` takes the hovered element's own `getBoundingClientRect()`, clamps horizontally so the tooltip can't run off the left or right edge, and flips `preferredSide` to the other side if there isn't `estimatedHeight` of room before the top or bottom edge. Neither tooltip was doing any of this before: `LogoCarousel`'s was already `position: fixed` (needed to escape the marquee's `overflow-y: hidden`) but only ever computed "above"; `HeroDiagram`'s was still the original `position: absolute` span from before any of this session's fixes, anchored to the hotspot with no notion of the browser window at all.
- `HeroDiagram.tsx`: converted to the same `position: fixed` pattern as `LogoCarousel` — a single tooltip element, positioned from whichever hotspot last fired `onPointerEnter`/`onFocus`, rather than eight separate `position: absolute` spans each carrying their own visibility. The per-hotspot `align`/`vertical` CSS classes (`--start`/`--center`/`--end`, `--above`/`--below`) came out entirely; horizontal centering and edge-clamping are `floatingTooltipPosition`'s job now, and the outward-by-default vertical preference moved into `heroDiagramHotspots` itself as an optional `tooltipSide`, so the same value that used to be computed from `top < 50` can be overridden per hotspot.
- `LogoCarousel.tsx`: `tooltipPosition()` replaced with a call to the shared utility, `preferredSide` hardcoded to `"below"` — the developer's explicit call, not "prefer above, flip if no room," since above is what caused the overlap in the first place. It still flips to "above" as a last resort if the window is short enough that below has no room either.
- Verified with puppeteer at a deliberately short viewport (700px tall, matching how short the developer's own browser window looked in their screenshots): Lyric's tooltip, which would have gone off the top at that height, now renders fully on screen, flipped below. Separately, at a normal height, the ICGC-ARGO carousel tooltip lands entirely below the diagram's own bottom edge — checked by comparing `getBoundingClientRect()` on both, not just by eye.
- "lectern should flip down so should score, arranger maestro and stage should flip up" — five explicit per-hotspot overrides against the generic top-half/bottom-half default, `data/heroDiagram.ts`'s new `tooltipSide` on Score and Lectern (`"below"`) and Maestro, Arranger and Stage (`"above"`), on the developer's own visual judgment of what reads best at each position rather than the generic rule. Verified each of the five individually: tooltip renders on the requested side of its hotspot.

Two more from the developer, and one final content pass to close out the day.

- **"Make the transition of the project highlighting smoother"** turned out to be about the marquee/filtered-view swap having no transition at all: `LogoCarousel.tsx` conditionally *mounted* the filtered view (`{matches && (...)`) and toggled the marquee with `visibility: hidden`, neither of which can animate — a freshly-mounted element has no prior frame to fade in from, and `visibility` is binary. Fixed by making both always-mounted, toggled with opacity classes (`&__viewport--hidden`, `&__filtered--visible`) and a 200ms transition, plus the same treatment on the edge fades so they crossfade with the marquee rather than popping. Verified the fade is real by sampling `getComputedStyle(...).opacity` at five points across the transition (1 → 0.57 → 0.27 → 0.07 → 0), not just eyeballing a screenshot.
- **A hint line, back but different**: says what the interaction is by default, same instruction text as the badge that got removed a few messages ago; while something's highlighted, names whichever side caused it — "Projects using Score" while hovering a hero component, "Products used in ICGC-ARGO" while hovering a carousel logo — computed in `LogoCarousel.tsx` from `highlightedComponent`/`highlightedPlatform` and rendered as plain text, not the floating badge from before (that idea stayed dropped). First pass used the house style's "Functional Name (Codename)" for the component half; the developer's own correction: "just use the branded name" — the codename alone (`hotspot.codename ?? hotspot.name`, falling back to the plain name for Control, which has no separate codename). Verified all three states directly: the default line, "Projects using Score" while hovering Score, "Products used in ICGC-ARGO" while hovering that card.
- Last: two more logo files, dropped at the repo root the same way as every other asset this session. The new IHCC one was an unambiguous match for `ihcc`. The one described as "ICGC argo" wasn't: it's the plain grey-and-white ICGC mark ICGC 25K already uses, not ICGC-ARGO's distinct colour wordmark — confirmed by comparing it directly against both platforms' current logos, not by trusting the description. Filed it as ICGC 25K's update and said so, rather than either guessing which platform was meant or overwriting ICGC-ARGO's real logo with the wrong mark.

One more request, in two parts: tighten the hero/carousel gap again, and stop shrinking the filtered view by a fixed amount at a fixed match count — scale it continuously, full size when it fits, only as small as it has to be otherwise.

- `_home.scss`, `_logo-carousel.scss`: `.Hero`'s padding and `.LogoCarousel`'s top padding both cut again (third time for each), plus the hint's own bottom margin.
- `LogoCarousel.tsx`, `_logo-carousel.scss`: `LogoCarousel__list--compact` (a fixed size switched on past 4 matches) replaced with `transform: scale()`, computed in a `useLayoutEffect` from the filtered list's own `scrollWidth` (its natural, unscaled width — reading this stays correct even after a transform is already applied, since `transform` doesn't affect layout size, so recomputing repeatedly doesn't compound) against the stage's `clientWidth`, capped at 1 so it only ever shrinks, never grows past normal size. A `ResizeObserver` on the stage keeps it correct if the window resizes while something's highlighted. Three or four matches now stay full size; Arranger, which matches eight of the nine platforms, shrinks only as much as it takes to keep all eight on one line.
- **A real bug, caught from a screenshot the developer sent of Arranger's hover specifically** (too large to view directly, so verified independently rather than asked to resend): matches were hanging off the right edge of the browser window. `getBoundingClientRect()` on the filtered list's items confirmed it — the last of Arranger's eight matches was measured at `left: 1598` in a 1440px-wide viewport. Root cause: `&__filtered`'s flex `justify-content: center` was centering the list *before* the JS scale-down, and a flex item wider than its own container doesn't center symmetrically when it overflows — it can spill further past one edge than the other. Scaling down around the wrong center point compounds that error instead of correcting it. Fixed by centering a different way entirely: `position: absolute; left: 50%; top: 50%` on the list plus `translate(-50%, -50%) scale(...)` in the same inline `transform`, which pins the list's own center to its container's regardless of how wide it naturally is, so the subsequent scale always shrinks toward the right point. Verified the same way the bug was found: `getBoundingClientRect()` on all eight of Arranger's matches, now landing between 95px and 1345px in the 1440px viewport, and a screenshot showing all eight logos on one line, fully visible, roughly centered.

One more from the developer, testing the fix above: "an odd zoom transition on Arranger's projects — should match the fade on the others." Real cause, not a rendering artifact: the filtered `<ul>` only renders while something's highlighted (`{matches && (matches.length > 0 ? <ul>...`), so it unmounts completely between hovers and mounts fresh each time. A fresh mount's first committed style is whatever `filteredScale` state happened to hold from the *previous* hover (state lives on the parent component, not the unmounted element, so it doesn't reset); the measuring `useLayoutEffect` corrects it to the right value a commit later, still before any paint reaches the screen — but `&--filtered`'s `transition: transform` didn't know the first value was never actually seen, and animated between the two commits anyway. Invisible when the two hovers happened to have a similar scale, obvious for Arranger specifically because its scale (0.63) is the smallest on record, so the correction was the largest. Fixed by removing the transition from `transform` entirely: the size is simply already correct on the frame that paints, and `&__filtered`'s own opacity fade is what animates the appearance now, same as everywhere else. Verified by sampling the computed `transform` at six points from 0ms to 200ms after hovering Arranger — identical at every sample, confirming there's no interpolation left to see.

Two small, quick requests to close: the hint text bigger (0.85rem → 1.05rem, sized and re-checked against a screenshot), and a bigger structural one — `LogoCarousel` as its own separate, narrow card rather than a full-width band sharing the hero's blue.

- `_logo-carousel.scss`: new `&__band` (`max-width: 760px`, centered, `border-radius: 24px`, `background-color: $blue-block` — the site's existing light-blue "block" token, already used the same way on `/products/`, and a solid match for the pale swatch the developer sent) wraps the hint and the stage; `.LogoCarousel` itself lost its own background and most of its padding, now just enough to give the card breathing room on the page rather than reading as an extension of the hero above it. The edge fades switched from fading into `$blue-top` to `$blue-block`, since that's what's actually behind them now.
- Nothing about the highlight/filter/scale-to-fit mechanics needed to change: `stageRef.current.clientWidth` (what the scale-to-fit math in LogoCarousel.tsx measures against) already reads whatever width the stage actually has, so it adapted to the narrower card automatically. Verified by hovering Arranger (all 8 matches) inside the new card: all eight land within the band's own left/right edges, scaled down and centered exactly the way they were in the full-width layout, just narrower.

Two content passes to close out the day: the seven component summaries against their own source repos, then the nine project summaries against each other.

- **"Review the submodules and come up with a more concise summary for each."** `data/components.ts`'s seven `summary` fields (what `HeroDiagram`'s tooltips and `/products/` both read from — the file's own header comment: "anything else that needs to name a component reads from here rather than restating it") were accurate but long, 23-30 words each. Read each component's own README in `submodules/` (real git submodules of the seven component repos, not just this docs repo's own account of them) before rewriting, rather than trimming the existing sentences blind, and cut each to roughly half length. That check surfaced two real content updates the shorter wording wouldn't have caught otherwise: Maestro's README now names both File Manager (Song) and Tabular Submission (Lyric) as sources it indexes, not File Manager alone; Arranger's now supports OpenSearch as well as Elasticsearch. Both corrected, not just shortened, with an inline comment on each marking it as a real update and the date it was confirmed.
- **"Update the project summaries to be more concise too, the pcgl and ohcrn summaries are appropriate lengths."** The five `LogoCarousel` tooltips sourced from a case study (added a few turns earlier, on request, to replace empty placeholders) were noticeably longer than OHCRN's and PCGL's, which had no case study to pull from and fell back to `platforms.ts`'s own `summary` field instead — the one that field's own comment already calls "one line, for scanning." Rather than hand-shortening five paragraphs, switched `partnerLogos.ts`'s default source for every platform to that same `summary` field and dropped the case-study lookup entirely: the long paragraph is written for a case study's own opening line, not a hover tooltip, and using the field the site already reserves for scanning contexts fixed the length for all five at once rather than one at a time. `caseStudies.tsx` is no longer imported here. Verified all five land in the same 10-18 word range as OHCRN and PCGL, including one (HCMI) that initially looked broken in testing — the marquee had scrolled it off the browser's viewport bounds again, same test artifact as ICGC 25K/GDC/Kids First earlier, not a real bug, confirmed by dragging it into view before checking.

Two more from the developer, in quick succession: widen the card back out to the full width (having just asked for narrow), and give it a real heading.

- `_logo-carousel.scss`: `&__band` lost its `max-width: 760px`/`margin: auto` centering, now filling `.container` at full width — the same `.LogoCarousel .container { padding: 0 32px; }` override from `_home.scss` that already lines its edges up with the hero's own content (the hero text's left edge, the diagram's right edge) does the rest, unchanged.
- `LogoCarousel.tsx`, `_logo-carousel.scss`: new `&__heading` wrapper holds an `H3`-based `&__title` reading "Powered by Overture" above the existing `&__hint` line, which keeps its own wording and now reads as secondary text under the title rather than the section's only line.
- The developer's own edit, found on a routine re-read rather than announced: `&__hint` picked up `text-align: left` and `padding-left: 47px` directly in `_logo-carousel.scss`. Left as found — not reverted, not asked about, since it renders fine: the paragraph is a flex child in a centered column with no explicit width, so it shrinks to its own content's width regardless of `text-align`, and the hint still reads centered under the title in every screenshot taken since.
- The developer then reported the card "still within the running in production since block" rather than reading as its own section, alongside a screenshot that failed to load on this end. Checked independently rather than asked to resend: `getBoundingClientRect()` on `.LogoCarousel`, `.LogoCarousel__band`, and `.HomeProof` at 768px, 1024px, 1920px all show the same shape — the two sections touch at a shared edge (one's `bottom` equals the other's `top`) but never overlap, and the band itself sits with visible padding inside its own section. Screenshots at all three widths show the card as a clearly separate light-blue block with white space both above (back to the hero) and below (down to "Running in production since 2016"). Could not reproduce what the screenshot was meant to show; reported back rather than guessing at a fix blind.

The developer's follow-up screenshot came through this time (the previous one hadn't loaded): the card, as built, still had white space to either side of it, all the way to the browser's own edges — not the "own section" problem from the earlier report, a different, more literal one: "this would span left to the right completely like the hero with no gap."

- `_logo-carousel.scss`: `background-color: $blue-block` moved from `&__band` onto `.LogoCarousel` itself, and `border-radius`/horizontal padding dropped from `&__band` — the same structure `.Hero` already uses one section up (`background-color: $blue-top` on the section, `.Hero .container`'s padding doing the indenting), rather than a rounded card floating inside the section's own white background. `&__band` is left as a vertical-padding-only wrapper. Verified with `getBoundingClientRect()`: `.LogoCarousel` spans the full viewport width at both edges (`left: 0`, `right`: viewport width) at 2000px, same as `.Hero` directly above it, with `&__band`'s content still inset to match the hero's own text/diagram edges via the container's existing padding.

**"accedently broke it please fix."** No image, no detail — `LogoCarousel.tsx` on disk had collapsed to an 80-line stub (just the marquee, none of the tooltip/highlight/scale-to-fit/drag work from this whole session), while `_logo-carousel.scss` was untouched. Not a git-recoverable loss (the file's untracked, added this session), so recovered from VS Code's own local History store (`~/Library/Application Support/Code/User/History/`, keyed by an opaque hash per file, found by grepping every history dir's `entries.json` for the file's real path) rather than rebuilding the whole component from memory. The closest snapshot (`xqyM.tsx`, 464 lines, saved via VS Code's own undo/redo three seconds before an even-further-undone save landed at the 80-line stub) had everything except the "Powered by Overture" heading added afterward — and one real bug of its own, a missing closing `</div>` on `.container` from whatever edit was in flight when it was captured. Restored from that snapshot with the tag fixed and the heading re-added, rather than the stub. Verified with a clean typecheck and build, then puppeteer: base view matches the last known-good screenshot, and hovering Control in the diagram still filters the carousel to its two matches, updates the hint to "Projects using Control", and shows the tooltip below the diagram rather than over it — the full interaction chain, not just that the page renders.

**"update the tooltips on the projects to show on top rather than below."** Reverses the earlier `preferredSide` fix directly: `floatingTooltipPosition(tooltip.rect, "above", ...)`, comment rewritten rather than left stale. That earlier fix existed because "below" was the fallback for a tooltip popping up and covering the hero diagram sitting right above the carousel — checked whether that's still true now that the carousel is edge-to-edge and further from the diagram than it was then. It isn't: the diagram sits on the right, tooltips render over the logo chips on the left/center, no overlap in any of three spot-checked positions. A different, smaller overlap does show up instead — a tooltip popping up from the row closest to the heading covers part of "Powered by Overture"/the hint text above it, since there isn't much vertical room between them. Left as requested rather than second-guessed; noted in roadmap.md as something to revisit if it reads as a problem live, not fixed unasked.

Then, from a review of the built page against `ia-proposal.md` § Home: the whole page below the hero and the carousel, rebuilt. Six bands became four, and the quickstart the Gatsby site had came back at the foot.

Measured before touching anything, at 1440px: 4763px tall, eight sections, and after the hero and carousel six bands of identical shape (centred H2, three columns of prose, roughly half of each band empty). The redundancy that made the cut obvious rather than a matter of taste: the product story was told three times (hero copy, "Modular, open, and yours to host", then Collect/Explore/Control with a six-line Control paragraph naming Keycloak, sitting directly under a diagram that shows the same grouping and names all eight components in its tooltips); impact was split across three bands with a product band wedged between them; collaborate appeared twice; and "Where to go next", the funders band and the footer were three endings in a row.

- `pages/home/index.tsx`: rewritten to Hero, LogoCarousel, Impact, Collaborate, Funders, Quickstart. `HomePillars` deleted outright, not moved: its copy is `distinctions` in `data/components.ts`, which `/products/` already renders in full under "Where Overture is different", so the argument is now made once on the page that exists to make it. The Collect/Explore/Control band went the same way, with the hero's "Learn More" carrying the one link to `/products/` this page needs. The three doors dissolved into the things themselves: "deploy it" is the quickstart, so it is a section rather than a promise of one; "collaborate with us" is its own band; "fund or partner" is the funders band that was already underneath it.
- The separate proof band is gone as a band and kept as content: its three figures are the credibility line for the three deployment cards, so they sit at the top of that section at `text-3xl` rather than in a band of their own at `4xl` with a heading of its own.
- `data/collaboration.ts`: `Offer` gained `oneLine`, which the new home band reads while `/collaborate/` keeps reading `blurb`. Written out rather than derived by splitting `blurb` on its first full stop: that breaks on "Overture." and reads like a truncation when it doesn't. Both live in the same file, so keeping them in step is a one-screen check.
- `_home.scss`: the band rhythm cut from `80px 32px` / `5.2em 3.8em` to `48px` / `3.2em`. Stacked with `.section`'s own 3rem, the old values put about 166px of blank space between one band's content and the next, which is what made every section read as half empty. Horizontal padding untouched. `.HomeProof`'s pull-up rule became `.HomeImpact`'s, since that is now the first band under the carousel.
- Deleted: `components/NoteBox.tsx` and `styles/components/_notebox.scss`, dead before the rebuild and a standing Known issue.

The quickstart itself, which is the part that reverses a decision rather than applying one. `ia-proposal.md` finding 5 cut the old Prelude walkthrough because a second copy of the setup goes stale the moment the first one moves, and the Gatsby site proves the point: it still publishes `git clone -b quickstart` and `make platform`, neither of which is how Prelude works now. Back on the developer's own call, with three things to make it survivable, and the risk written into `roadmap.md` § Known issues with a build-gate check attached rather than left as a good intention.

- `data/quickstart.ts`: new. The four steps as data, every command mirrored from `docs/deploy-docs/01-prelude.md` § Getting Started, which both files name as the source of record. Commands only; the one step whose detail lives elsewhere (prerequisites) carries a single link out instead of prose. One deliberate addition to the documentation's own snippet: `cd ../..` between building the Stage image in `apps/stage` and running `make phase1`, which that page describes in words ("from the root directory") and omits from the block, so anyone copying it lands in the wrong directory.
- `components/Terminal.tsx`, `styles/components/_terminal.scss`: new, ported from the Gatsby `Terminal` the phase 1 port left behind with the walkthrough. Kept: the frame, the traffic lights, `$terminal-text` on `$grey-darkest` (9.8:1, measured, so it clears AA). Changed: one `<pre><code>` rather than a `<div>` per line, so a hand selection keeps the newlines; and a copy button, which is how anyone actually takes a command. The original's `UrlBar` was not ported, since nothing uses it.
- `components/HomeQuickstart.tsx`, `styles/components/_quickstart.scss`: new. Numbered steps in the wide column, the portal screenshot (`overtureQuickstartPortal.webp`, already in `static/`) sticky beside them at `desktop-up` and `display: none` below it, where two columns would leave the commands too narrow.
- `pages/home/index.tsx`, `components/Link.tsx`: the hero's "Get Started" now points at `#quickstart` rather than out to the docs, so the page's first CTA and its last section are the same offer. `Link` gained a branch for a bare hash: react-router resolves one against the current path and pushes a history entry that lands on the same page without scrolling, so a hash on its own goes to the browser instead. Verified by clicking it, `scrollY` lands exactly on the section's own offset.
- `pages/collaborate/index.tsx`: every `id` on that page now registers with `collectAnchor`. The new home band links into `/collaborate/#academic-partnership` and its two siblings, and the build reported all three as broken anchors: a plain React page registers none on its own, exactly the gap `ProductGroup` hit when `HeroDiagram` first linked into `/products/#lyric`. Nothing had linked into `/collaborate/`'s ids until now. Build is back to zero warnings.

Three things were found by measuring rather than by reading the CSS, one of them after two failed fixes.

- **A hyphen is a line-break opportunity in CSS**, so `docker build --platform linux/arm64 ...` rendered as `docker build --` / `platform` on a 390px column: a flag split into what reads as two arguments. `overflow-wrap: break-word` did not fix it (the break is not an overflow-wrap question) and neither did `word-break: keep-all`, which produced the identical break in Chrome. What fixed it: each whitespace-separated token is its own `white-space: nowrap` span, so the only break opportunities left in a command are the spaces the command itself has, plus a hanging indent (`padding-left: 2ch; text-indent: -2ch`) so a wrapped line is visibly a continuation rather than the next command. An intermediate version used `white-space: pre` with sideways scrolling, which was correct but left the docker command visibly cut off on a phone with no affordance; reverted. Every step of this was checked with Range rects per visual line, not screenshots, because a screenshot cannot tell a wrap from a real newline.
- **The copy button moved from over the code into the title bar.** It was `position: absolute` over the top right of the code block with 64px of reserved padding, which is fine until the block scrolls sideways, at which point lines pass underneath it. In the bar it also stops needing light-on-dark colours.
- **The `<code>` in "Your portal is then at localhost:3000" was Infima red.** The partial set its font, background and padding but not `color`, and an unstated property still comes from Infima's rule even where `.marketing` keeps ours winning on the rest.

Verified: `typecheck` and `build` clean with zero warnings; every new class name grepped against the emitted stylesheet (the `ow:sm:grid-cols-3` variant included) so nothing resolves to no CSS; no horizontal body overflow at 390, 900 or 1440; the quickstart's screenshot column and the hero diagram both absent at 390 rather than merely invisible. The copy button's logic was verified with the clipboard API stubbed in the page (label flips to "Copied", the exact four commands arrive newline-joined, reverts after 2s) because headless Chrome refuses `clipboard.writeText` outright, permissions override or not: that refusal is the environment, not the component, and the real clipboard write is the one thing here not exercised end to end. Section heights at 1440 now: hero 479, carousel 296, impact 693, collaborate 740, funders 470, quickstart 1598. The three prose bands total 1903px where the six they replaced totalled 2716px, and the page is the same overall height as before with a 1598px quickstart added to it.
