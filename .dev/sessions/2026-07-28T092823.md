Acted on the Workstream B verify reports (the fix phase), correcting each component's docs against its currently checked-out code on its doc branch, plus this repo's deploy docs and embedded API specs; all edits left uncommitted per principle 4, site build green under onBrokenLinks:throw.

- `submodules/stage/docs/setup.md`: fixed the Critical setup blocker (`mv .env.stage .env` targeted a non-existent file) to `cp .env.schema .env` and reframed the "preconfigured" prose as a copy-and-populate template; dropped phantom `NEXT_PUBLIC_SHOW_MOBILE_WARNING`; repointed the stale quickstart link to `/deploy/quickstart`
- `submodules/arranger/docs/`: fixed the Critical `.env` blocker (`mv .env.arrangerDev .env` to `cp apps/search-server/.env.schema .env`), `CONFIG_PATH` to `CONFIGS_PATH`, documented the shipping `execute-query` MCP tool and removed it from "What's coming", de-indented the `:::info` admonition and repo tree that were rendering as code blocks, corrected the `integration-tests/` tree, added the `DOWNLOAD_MAX_ROWS`/`ALLOW_CUSTOM_DOWNLOAD_MAX_ROWS`/`DOWNLOAD_STREAM_BUFFER_SIZE` defaults, and repointed three stale cross-links to root-relative paths
- `submodules/maestro/docs/`: rewrote the Critical Score-copy-paste Verification section (port 8087 to 11235, `/download/ping` to `/`, `/swagger-ui.html` to `/api-docs`, "Score" to "Maestro" throughout setup.md), added the `MAESTRO_` prefix to the Slack and exclusion-rules env vars so they bind, corrected the local Swagger URL to `/api-docs`, and repointed stale cross-links
- `submodules/song/docs/`: dropped three non-existent `search` flags, removed unsupported federated file/analysis URI templates and fixed the auth-URL var to `ID_FEDERATED_AUTH_BEARER_CREDENTIALS_URL`, changed `SPRING_CONFIG_ACTIVATE_ON_PROFILE` to `SPRING_PROFILES_ACTIVE`, rewrote the authentication scope block from Score's DOWNLOAD/UPLOAD vars to Song's `STUDY_PREFIX`/`STUDY_SUFFIX`/`SYSTEM` and `AUTH_SERVER_INTROSPECTIONURI`, repointed the config link from the Score repo to Song, `song-client` usage to `sing`, moved `song-java-sdk` out of "unsupported", replaced a personal machine path with a placeholder, and fixed cross-links, the swagger URL, and a stray glossary character
- `submodules/score/docs/`: removed the non-existent `upload --validate` and `view --output-file` options, un-inverted the OAuth system scopes (download `score.READ`, upload `score.WRITE`), renamed the scope study vars to the per-operation `_{UPLOAD,DOWNLOAD}_STUDY_{PREFIX,SUFFIX}` forms, changed the non-existent `aws` profile to `s3` and `S3_ACCESS_KEY` to `S3_ACCESSKEY`, noted only `TRIMMED` output-type is implemented, and fixed swagger URLs, a "Songs API" copy-paste, and a typo
- `submodules/lectern/docs/`: corrected the packages list (`common` does not exist, added `ui`), de-indented and repaired the malformed components table (shifted Dictionary row, wrong Validation badge link, truncated row), documented `MONGO_URL` and its precedence over the discrete `MONGO_*` vars, repointed stale Lyric links, and removed the tracked `docs/.DS_Store` (added to `.gitignore`)
- `submodules/lyric/docs/`: repointed eight stale `docs.overture.bio/build/core-software/<X>/overview` cross-links to root-relative `/develop/<X>/overview`
- `website/docs/deploy-docs/03-deployment/03-search-portal.md`: removed the stray chatbot artifact, corrected the inverted analysis-centric/file-centric prose, fixed `TRUSTSELFSIGNCERT` to `TRUSTSELFSIGNEDCERT`, dropped the phantom Stage `NEXT_PUBLIC_SHOW_MOBILE_WARNING`/`NEXT_PUBLIC_SCORE_API_URL`, and added a "Verified against" provenance note (Maestro 4.3.0, Arranger 3.1, Stage 1.1.3, ES 7.17.1)
- `website/docs/deploy-docs/03-deployment/02-file-transfer.md` (renamed from `02-data-management-storage.md`, see below): fixed the Kafka bootstrap-servers scheme, repointed the two `/` cross-links to Song reference pages, corrected the SCORE_URL and swagger paths, dropped the non-existent `collaboratory`/`jwt` profiles and `AUTH_JWT_PUBLICKEYURL`, renamed the Score scope study vars to per-operation forms, corrected the MinIO port and client-id prose, and added a "Verified against" note (Song 5.1.2-SNAPSHOT, Score 5.12.0-SNAPSHOT)
- `website/src/components/SwaggerAPIDoc/lyricAPI.json`: removed the four confirmed-absent paths (three `/migration/*` and `/validator/.../exists`); the seven under-mounted-router endpoints left pending confirmation
- `.dev/submodule-branches.md`, `.dev/roadmap.md`: updated the branch accounting dirty counts and marked the Workstream B fix phase status

Acted on the maintainer's follow-up: pinned current Arranger/Song images and reconciled the Arranger deploy block to 3.1, confirmed the TypeScript Maestro as canonical, authored the missing Lectern/Lyric deploy guide, and split the Song/Score deploy page out as File Transfer.

- `website/docs/deploy-docs/03-deployment/03-search-portal.md`: Arranger image to `ghcr.io/overture-stack/arranger-search-server:3.1.0-rc.2` with a 3.1 env block (dropped the non-3.1 `REACT_APP_*` vars, added `SEARCH_ENGINE` and `CONFIGS_PATH`), a single directory config mount, and `matchbox.json` removed (not a 3.1 config file); Song image to edge `fc479c13`; provenance note updated to the pinned Arranger image
- `website/docs/deploy-docs/03-deployment/02-file-transfer.md`: renamed from `02-data-management-storage.md` and retitled "File Transfer" so Song/Score parallel the new tabular-submission page and match the `docs-demo/file-transfer` naming; Song image to edge `fc479c13`
- `website/docs/deploy-docs/03-deployment/04-tabular-data-submission.md`: new deploy guide, Lectern (MongoDB) then Lyric (Postgres, pointing at Lectern via `LECTERN_URL`), closing the lectern/lyric deploy-coverage gap; images `ghcr.io/overture-stack/lectern:latest` and `ghcr.io/overture-stack/lyric:latest`; provenance note (Lectern 2.0.0-beta.3, Lyric 0.3.0)
- `website/deploySidebars.ts`, `website/docusaurus.config.ts`, `website/docs/deploy-docs/index.md`: sidebar gains the tabular page plus the file-transfer rename; redirects from the old `data-management-storage` slug (and its legacy `/guides` path) to `/deploy/deployment/file-transfer`; index walkthrough and "coming soon" note updated
- `website/docs/develop-docs/08-api-reference/03-maestro.mdx`: net unchanged. The canonical implementation is the TypeScript `@overture-stack/maestro-server` (repository/organization/id, matching the committed `maestroAPI.json`), not the vendored Java submodule, so the original Key Features prose stands. Caveat: the vendored maestro developer docs (and the fix-phase edits to them) describe the Java implementation. The Slack-notifications page (`reference/05-slack-notifications.md`) is marked `draft: true` since the canonical TS server has no Slack; reorienting the rest of the maestro docs to TS remains open
- `submodules/maestro/docs/reference/05-slack-notifications.md`: added `draft: true` frontmatter to exclude it from the production build (canonical TS maestro has no Slack integration), with a maintainer note to unset once TS reaches parity
- site build green under `onBrokenLinks:throw` after each batch

Integrated the search-portal workshop with AI-Assisted Data Discovery: added reciprocal cross-links and re-platformed the workshop's Arranger onto the multicatalogue `arranger-search-server` package (single catalogue kept), smoke-tested end to end.

- `website/docs/use-docs/ai-assisted-data-discovery/index.md`, `05-deploy-your-own-mcp-server.md`: added reverse cross-links to the workshop (`/use/workshop/prerequisites`); the forward link already existed in workshop `09-Next-Steps.md`, so both directions are now explicit
- `website/docs/use-docs/workshop/05-Docker-Configuration.md`: Arranger section re-platformed to `arranger-search-server:b5c6051b` (single `arranger` service, parent config dir mounted at `/app/configs`, `ES_ARRANGER_SET_INDEX: arranger_set`); reframed "one Arranger per data table" as "one service, one config dir per catalogue"; added a catalogue-routing note (single catalogue at `/graphql`, multiple at `/<catalogue>/graphql`)
- `website/docs/use-docs/workshop/01-Running-the-Demo.md`, `07-Troubleshooting.md`: service/container renamed `arranger-datatable1` to `arranger`; fixed the GraphQL example query from `{ datatable1 { ... } }` to `{ records { ... } }` (root field is the `documentType`, verified against the running stack returning 250 records, and already correct in `07`)
- prelude `docs-demo/search-portal-workshop` (separate repo, uncommitted): `docker-compose.yml`, `Makefile`, `README.md`, `setup/configs/nginxConfigs/workshop-nginx.conf`, `setup/scripts/services/arranger/arranger_check.sh` migrated to `arranger-search-server`; the check script ported from the ai-assisted branch (auto-discovers catalogues by scanning the config dir); `make demo` green (setup exit 0, 250 records at `/graphql`, `/datatable1/graphql` 404s, Stage reaches `arranger:5050`). Stage container reports `unhealthy` from its pre-existing `wget`/localhost-IPv6 healthcheck (Next binds IPv4 `0.0.0.0`), unrelated to Arranger: app serves 200, RestartCount 0
- `.dev/roadmap.md`: marked the cross-link and arranger-search-server re-platform items done; logged the multi-catalogue workshop expansion as a future item
