Reoriented the vendored Maestro developer docs from the Java/Spring implementation to the TypeScript `@overture-stack/maestro-server` (V5), the canonical implementation. Verified every claim against the V5 source, shallow-cloned from the `M5-revised` branch (V5 is not yet on `master`), rather than the checked-out Java submodule (the roadmap-sanctioned exception to principle 2). All edits on the maestro `overtureDocsUpdate` branch, uncommitted (principle 4).

Source verification (against `M5-revised`, not the M5 branch's own docs prose, which still advertise features the code lacks):
- REST routes (`apps/server/src/routes/indexer.ts`, `server.ts`): `POST /index/repository/:repositoryCode`, `.../organization/:organization`, `.../organization/:organization/id/:id`; health at `GET /health` returning `{uptime,message:"OK",timestamp}` (not `GET /` `{"status":"up"}`); Swagger at `/api-docs`; success is HTTP 202 `{successful,indexName}` (`indexerController.ts`, `maestroAPI.json`)
- Config is env-var only, `MAESTRO_*` prefix (`config/envConfig.ts`, `repositoryConfig.ts`, `provider.ts`); no `application.yml`, no `.env.maestro`. Repositories are `MAESTRO_REPOSITORIES_<n>_*`, typed `SONG` or `LYRIC`; `SONG_INDEXING_MODE` values are `file`/`analysis` (`packages/common/src/types/config.ts`)
- **Slack and exclusion rules are absent from the V5 code** (grep of `apps/`+`packages/`): both appear only in the repo README and the branch's own `docs/` prose, carried over from Java. Confirms the earlier Slack `draft:true` call and drove the same treatment for exclusion rules
- Kafka (`packages/maestro-provider/src/kafka/consumer.ts`, `processMessage/requestMessage.ts`): one request topic (`MAESTRO_KAFKA_INDEX_REQUEST_TOPIC`) + per-repo document topics (`MAESTRO_REPOSITORIES_<n>_KAFKA_DOCUMENT_UPDATE_TOPIC`); request payload still uses `studyId`/`analysisId` field names, mapped to organization/record internally; Lyric documents routed by `LYRIC_CATEGORY_ID` (id or alias, equality match)

Edits (`submodules/maestro/docs/`, 7 files):
- `overview.md`: Spring Boot app → Express/pnpm monorepo (`apps/server` + `packages/{common,indexer-client,maestro-provider}`); added Lyric as a second source alongside Song; indexing levels repository/organization/record; dropped the Java `maestro-app`/`maestro-domain` tree and the Exclusion Rules key-feature bullet; mermaid now shows Song + Lyric feeding Maestro
- `setup.md`: JDK11 + Maven → Node 22 + pnpm; supporting services via the maestro repo's own `make docker-start-dev` (ES7 + Kafka from `apps/server/docker-compose-es7.dev.yml`) instead of prelude `make maestroDev` (prelude V5 support unverified); `.env` from `apps/server/.env.example`; `pnpm install` + `pnpm run build:all` + `pnpm run start:dev`; verification hits `GET /health`. Added a maintainer HTML comment: clone instructions assume V5 is the default branch, which it is not yet
- `reference/01-indexing-data.md`: `study`/`analysis` paths → `organization`/`id`; explained organization = study (Song) / organization (Lyric); Swagger steps updated to the `Indexing` tag; 202 `{successful,indexName}` response. Dropped the three Swagger screenshots (`index-analysis.png`, `index-study.png`, `index-repo2.png`) since they show the removed Java `management-controller`/`study` endpoints; they are now orphaned tracked assets
- `reference/02-exclusion-rules.md`: `draft:true` + maintainer note (feature absent in V5 code), mirroring the Slack page
- `reference/03-index-mappings.md`: mode via `MAESTRO_REPOSITORIES_<n>_SONG_INDEXING_MODE`; removed the Java `maestro-app/.../file_centric.json` link and `.env.maestro`; migration flow via `INDEX_NAME` + ES `/_reindex`
- `reference/04-kafka-topics.md`: full rewrite from Spring `application.yml` to the `MAESTRO_KAFKA_*` + per-repo document-topic env model; request-topic payloads (repo / organization+study / record + `remove`), Song and Lyric document shapes, Lyric category routing (shared topics, fan-out, no-match skip)
- `reference/reference.mdx`: glossary gained Organization, Song, Lyric, Record, Category; dropped the Exclusion Rules term
- `reference/05-slack-notifications.md` and `06-api-reference.mdx`: unchanged (already draft / already TS via `maestroAPI.json`)

Build: Maestro pages pass `onBrokenLinks:"throw"` (the build reaches the redirect postBuild stage, which runs after the broken-links check, before failing). No maestro page has a broken link, and nothing links to the two now-draft pages (grep confirmed).

Pre-existing build blockers, unrelated to Maestro, surfaced while verifying (from other uncommitted in-flight deploy work, so the roadmap's "site build green" line is stale):
- redirect `to:/deploy/deployment/authorization` in `docusaurus.config.ts` has no target page (authorization was superseded by keycloak)
- `/deploy/deployment/score` links to the missing `/deploy/deployment/authorization`
- `/use/ai-assisted-data-discovery/deploy-your-own-mcp-server` links to the missing `/deploy/deployment/mcp-server`
Left untouched (the human's in-flight deploy files); flagged for a maintainer.

`.dev` accounting: updated `submodule-branches.md` (maestro row `d831878`/6 → `e712237`/7; the Workstream B maestro fix is now committed as `e712237`) and `roadmap.md` (Maestro V5 item marked done with residuals; `.env.maestro` deferred bullet resolved for the develop docs).
