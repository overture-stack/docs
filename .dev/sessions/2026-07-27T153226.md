Completed roadmap A1.1's guide/feature naming so the site builds again: the AI-Assisted Data Discovery page had been renamed but two workshop links still pointed at the retired `/use/conversational-data-discovery` slug, failing `docusaurus build` under `onBrokenLinks: "throw"` (the committed site did not build).

- `website/docs/use-docs/workshop/02-Architecture.md`, `website/docs/use-docs/workshop/09-Next-Steps.md`: repointed the "AI-Assisted Data Discovery guide" links to `/use/ai-assisted-data-discovery`, clearing the broken-link build failure (verified green)
- `website/docs/use-docs/ai-assisted-data-discovery.md`: retired the stray "Conversational Data Discovery" term in the "Try it locally" section
- grant-initiative "Conversational Data Discovery (CDD) platform" references left as-is (workshop section headings, platform prose, the alliancecan.ca link, the mockup admonitions): they name the DRAC Objective 1 platform, a maintainer decision, not the guide
- `.dev/roadmap.md`: A1.1 marked in progress with the above outcome and the one open naming decision

Recorded a no-em-dashes house style for authored docs prose and conformed all existing authored content to it; each em dash replaced with the comma, semicolon, colon, or parentheses that fits its sentence.

- `.dev/roadmap.md`: added operating principle 5 (no em dashes in authored documentation prose; use commas or semicolons where appropriate)
- `website/docs/use-docs/ai-assisted-data-discovery.md`, `use-docs/administration/01-building-dictionaries.md`, `use-docs/workshop/05-Docker-Configuration.md`, `use-docs/workshop/09-Next-Steps.md`, `use-docs/workshop/00-Intro.md`, `use-docs/workshop/10-Extension-Task.md`, `deploy-docs/01-prelude.md`, `src/pages/index.tsx`: replaced all 30 em dashes in authored prose with sentence-appropriate punctuation
- code comments left as-is, outside the "authored prose" scope: `src/css/custom.css` (3), `communitySidebars.ts` (1)

Extended the human-run git rule so branch creation joins commit and push: the workflow now pauses and waits for the developer to create any needed branch before proceeding.

- `.dev/roadmap.md`: operating principle 4 rewritten to cover commit, push, and new-branch creation as human-run, with the workflow naming the branch it needs and why, then waiting; also dropped the em dashes from the principle's own wording

Proceeded with Workstream A / Phase A1: expanded the AI-Assisted Data Discovery guide from a concepts-only page into a deployable pilot-user guide covering the grant deliverables (testing environment, per-OS installation, configuration templates, demonstrated use cases, LM Studio integration), grounded in the Prelude `overtureMCP` branch docs.

- `website/docs/use-docs/ai-assisted-data-discovery.md`: added Set up the testing environment (prerequisites per OS, `make demo`, service/port table, demo-data coverage, verification), Connect a host application (LM Studio install, tool-capable model selection, MCP connection), Configuration templates (server entry, recommended prompts, connection-testing procedure and troubleshooting), Demonstrated use cases (iterative refinement in full; pathway-informed filtering as the gene-refinement half with the pathway lookup flagged forthcoming), and Deploy the MCP server against your own Arranger (docker run, env-var reference, log-sensitivity warning). Windows path is WSL2 only, verified against the branch having no `run.ps1`; every command, port, catalogue, and env var taken from the Prelude `user/`/`admin/` docs, not invented. Build green
- `.dev/roadmap.md`: A1 status note added; A1.3/A1.4/A1.5/A1.7 marked done, A1.6 partial (pathway half blocked on ITCR MSigDB tooling)

Split the AI-Assisted Data Discovery guide from one long page into a navigable folder of pages, preserving the `/use/ai-assisted-data-discovery` landing route so inbound links stay valid.

- `website/docs/use-docs/ai-assisted-data-discovery/`: new folder with `index.md` (overview: concepts, principles, workflow, in-this-guide index, what's coming) plus five numbered pages (testing environment, connect a host, configuration templates, use cases, deploy your own server); the old in-page `#anchor` links converted to cross-page links, and the "add the server entry below" step reworded since the templates now live on their own page
- `website/docs/use-docs/ai-assisted-data-discovery.md`: removed (content moved into the folder; keeping both would collide on the same route)
- `website/useSidebars.ts`: the single Data Consumers doc entry replaced with a nested category linked to the folder index, mirroring the workshop's category pattern

Reviewed the ITCR meeting deck (`.dev/apps/[12 min version] Overture ITCR Meeting 2026 (3).pptx`), its speaker notes, and its slide visuals, and used them to refine and correct the guide.

- `website/docs/use-docs/ai-assisted-data-discovery/index.md`: recast the researcher workflow into the four-beat lifecycle (request, clarifying intent, confirm, execute) with the model's classify step (answerable / ambiguous / unanswerable / improper) and its text-to-SQL source (Guo et al. 2024, arXiv:2412.17867); added a "why conversational discovery" motivation (escaping the per-tool integration trap) and a self-describing / portability note (the same MCP server works on any Overture deployment); refocused "what's coming" on the research-environment direction (reproducible code artifacts, Skills, the MSigDB pathway server)
- `website/docs/use-docs/ai-assisted-data-discovery/01-testing-environment.md`: added the upstream Drug Discovery figures (about 405M records, ~20K genes, 32 cancer types; datasets joined by gene HUGO symbol)
- `website/docs/use-docs/ai-assisted-data-discovery/02-connect-a-host.md`: added the local-model target range (~4B to 70B parameters, Q4-quantized)
- `website/docs/use-docs/ai-assisted-data-discovery/04-use-cases.md`: corrected use case 2 against the demo's real coverage after viewing the slide mockup, the deck's COADREAD/`hotspot` example illustrates the full portal, not the BRCA-only demo sample, so the reproducibility claim now points readers to BRCA and documented fields; framed the ambiguous-ask to clarify flow; named the forthcoming pathway hand-off as an MSigDB MCP server
- deliberately excluded the deck's team member names and personal `@oicr.on.ca` email, and did not embed the deck's mocked-UI screenshots (labelled "for presentation purposes"), to avoid personal info in a public repo and presenting mocks as the real demo

Acted on the maintainer's roadmap replies: completed the guide-name lock across the site (A1.1) and inventoried the Prelude-repo items (A1.2, A1.8) before editing another repo.

- `website/docs/use-docs/workshop/02-Architecture.md`, `09-Next-Steps.md`: retired the last "Conversational Data Discovery / CDD platform" references (section headings, the alliancecan-linked term, the mockup alt text and admonition, the compatibility note), renamed to "AI-Assisted Data Discovery" per the maintainer's decision; no CDD term remains in authored docs, build green
- `.dev/roadmap.md`: A1.1 marked done; the A0 scheme marked approved with the branch renames left to the maintainer (principle 4, outward-facing shared repo); A1.2 and A1.8 marked in progress with inventory findings; the maintainer's inline reply comments folded in and removed
- A1.2 / A1.8 inventory (read-only, from prelude's own `.dev/roadmap.md` + `tech-debt.md`): the OvertureMCP branch is clean and backs the demo the guide references, and the guide was authored from prelude's `user/`/`admin/` docs with no contradictions. Most outstanding prelude work is prelude-project scope (fixture-corpus validation, the dictionary-hydration stub, publication decisions) best driven from a prelude session. The one guide-coupled defect: arranger's `apps/mcp-server/README.md` lists query execution as "Not Implemented Yet" although `execute-query` is implemented, and both this guide and prelude's docs cite that README as canonical; it is a Workstream-B arranger-doc fix

Applied the approved A0 rename to the guide and fixed the guide-coupled arranger README defect.

- `website/docs/use-docs/ai-assisted-data-discovery/01-testing-environment.md`: clone command repointed from `-b overtureMCP` to `-b docs-demo/ai-assisted-data-discovery` after the maintainer renamed and pushed the branch (`origin/docs-demo/ai-assisted-data-discovery` confirmed present). Docs build green
- `submodules/arranger/apps/mcp-server/README.md` (arranger, branch `overtureMCPdocsUpdate`): removed the stale "query execution tools" from "Not Implemented Yet" and added an accurate four-tool table (`list-catalogues`, `get-sqon-schema`, `get-catalogue-fields`, `execute-query`), verified against `src/mcp/tools.ts` and `executeQueryTool.ts`; this is the canonical reference both the guide and prelude link to. Arranger-repo change: commit on its doc branch, then bump the docs arranger pin
- `.dev/roadmap.md`: A0 note updated (rename applied for guide 1; pruning old branch, `docs-demo/data-submission`, and per-branch READMEs remain); A1.2 note updated (README defect fixed on the doc branch, pending commit + pin bump)

Built the File Submission + File Download demo (roadmap A2): adapted the PPCG example in place into the Prelude `docs-demo/file-transfer` branch as an Overture-generic Song/Score portal with dummy data, then rewrote both docs-site guides to use it.

- `apps/prelude` (branch `docs-demo/file-transfer`): removed the MCP demo (arranger submodule, arranger-mcp/opensearch/lectern/postgres configs, MCP data + docs, `conductor`, top-level `setup/`) and laid down the genericized PPCG stack (Song, Score, Maestro, MinIO, Kafka, Arranger, Stage). Compose project set to `overture-demo` so the network is deterministically `overture-demo_platform-network` regardless of clone directory; study `ppcg`→`demo`. Not started here (multi-service Docker not run); the maintainer should `make demo` to smoke-test before publishing. Branch left uncommitted (principle 4)
- `apps/prelude` privacy + branding: scrubbed two personal `@oicr.on.ca` emails (compose `NEXT_PUBLIC_ADMIN_EMAIL`, stage `HELP_URL`) to `admin@example.com`; portal branding "PPCG/PanProstate" → "Overture Demo Portal" across compose, setup banners, README, and stage components
- `apps/prelude` data model: dropped the prostate-specific `gleason_score`/`gleason_pattern` fields from the `genomicVariants` schema, the arranger extended/facets/table configs, the es file-mapping, and both admin docs; regenerated dummy data as 3 donors (`DO001` breast, `DO002` lung, `DO003` colorectal), 12 placeholder files (snv/indel/cnv/sv), new payloads + `clinical_data.csv`
- `apps/prelude` README + `docs/user/01-Data-Submission.md`, `docs/user/02-Data-Download.md`: rewritten Overture-generic; clone points at `docs-demo/file-transfer`; clients join `overture-demo_platform-network` by hostname with a fixed demo token (auth disabled)
- `website/docs/use-docs/03-cli-submissions.md`, `website/docs/use-docs/02-cli-downloads.md`: rewritten to clone the demo branch and mirror the instance docs; dropped the Keycloak API-key flow (auth is disabled in the demo) and the old `composer` / `--network=host` / `quickStartSchema` workflow; no em dashes; `docusaurus build` green
- `.dev/roadmap.md`: A2.1–A2.6 marked done with adaptation details and a runtime-unverified flag; guides 2 & 3 table rows renamed `data-submission` → `file-transfer` and set to 🟡; A0 note updated
- the `pcgl-dictionary.json` under `apps/prelude/apps/stage/public/dictionary` is left as-is: it is an unreferenced Lectern-viewer demo fixture (PCGL, not PPCG), not wired into any page
