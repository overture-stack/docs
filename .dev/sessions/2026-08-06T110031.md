Staged the Gatsby site inside the docs repo so the port can start there, on the `overtureWebsiteUpdate` branch. Nothing is committed in either repo. (Recovered from the staged `website-legacy/.dev/` copy before that duplicate was deleted later the same day; this is the docs-repo side of that work, so it belongs here.)

- `website-legacy/`: the site's 343 tracked files, copied with `git archive HEAD` rather than a recursive copy, so build artifacts could not come along. Verified that `node_modules/`, `.cache/`, and `public/` are absent and that the file list matches the source exactly
- Placed beside `website/`, never inside it: Docusaurus turns files under `website/src/pages/` into routes, so staging there would publish the Gatsby sources as pages. Confirmed the docs site still builds afterwards, 382 pages, zero warnings under `onBrokenLinks: "throw"`
- `website-legacy/README.md` opens with a staging notice (not built here, deleted after the port, read the roadmap first)
- `AGENTS.md` now describes `website-legacy/` in its repository orientation, including the warning to keep it outside `website/`
- Flagged rather than resolved at the time: the docs repo had no top-level licence, and this code is AGPL-3.0-or-later, so the Gatsby `LICENSE` was copied alongside to keep provenance with the code. Settled later the same day, below
- Superseded: the staged copy originally carried its own `.dev/` (tracked, because this repo's ignore rule is root-anchored). That duplicate was deleted; the Gatsby repo's own `.dev/` is the copy of record, since that repo still owns and deploys the site until stage 4 archives it

---

Rewrote `.dev/roadmap.md` to be forward-facing, then narrowed it to this branch and folded `tech-debt.md` into it.

- **Corrected the state it described,** by checking rather than trusting it: the working tree is clean, so every "uncommitted" item had landed; the maestro pin was already past the commit the file was waiting on. The real gate is [docs#48](https://github.com/overture-stack/docs/pull/48), blocked on five open component PRs (Lectern, Lyric, Song, Score, Maestro), with arranger#1089 and stage#257 merged. Verified with `git merge-base --is-ancestor` that the `submodules/arranger` (`594863e3`) and `submodules/stage` (`1bd835f7`) pins are **not** reachable from their `origin/main`, so both need re-pointing now that those PRs are in
- **`tech-debt.md` deleted, all eight entries carried over first:** the broken `npm run typecheck` and the missing CI build gate were promoted into the port's critical path, since the port lands a large amount of new component code in a repo with no type gate; the other six went to a Parked section
- **Scoped to the port.** Everything else this repo has open (docs v2 publishing, grant context, cross-component setup consistency, content work, maestro residuals, arranger upstream, cross-repo docs debt) is one Parked paragraph each. The hierarchical-generation design note was kept in full rather than compressed, since it exists nowhere else and `.dev/` is untracked
- **Verified the port plan's claims** against this repo instead of copying them: `onBrokenLinks: "throw"` at `docusaurus.config.ts:21`, the swizzled `src/theme/Layout` mounting `MatomoTracking` on siteId 76 (so `gatsby-plugin-matomo` genuinely just disappears), no `sass` in `website/package.json`, and `website/src/pages/` still holding only `index.tsx`
- Dropped the `prompts/` and `reports/` references: those directories no longer exist. `tech-debt.md` had a dangling link to `prompts/update-deploy-docs.md`, which went with the file

---

Added a root `LICENSE`: AGPL-3.0, the verbatim FSF text, byte-identical to the copy in the website repo and in stage, lectern, and maestro (`md5 4ae09d45`). Every sibling repo carries the same licence and the site's own `community-docs/03-licensing.md` already declares Overture software AGPL-3.0. Filename is plain `LICENSE` to match the majority; only score uses `license.md`. No appended copyright holder line, matching the siblings. `README.md` gained a License section noting that vendored `submodules/` content stays under its own repository's licence.

---

Recorded two decisions from the maintainer and reworked the plan around them.

- **The two hostnames keep their current roles.** `overture.bio` stays the website, `docs.overture.bio` stays the documentation site. No cutover, no path-for-path 301, so every docs URL `docs#48` creates is permanent. Stage 3 was rewritten from "cut the domain over" to serving the ported pages at `overture.bio`, and three downstream notes inverted: the `/documentation/*` redirects stay cross-host rather than becoming same-host targets, marketing-to-docs links stay absolute cross-host URLs that `onBrokenLinks` cannot check (so the stage 0 URL-check script becomes permanent tooling), and the apex-versus-`www` question no longer resolves itself, since Docusaurus needs one explicit `url`. Left as the one open sub-question: how two hostnames come off one repo. Recommended two Netlify sites separated by a build-time flag, with the single-site alternative rejected because it publishes every page on both hosts
- **The products page covers the whole stack:** Lectern, Lyric, and Stage join Song, Score, Maestro, and Arranger. Re-checking the staged copy showed this is now purely additive: `ProductsPageSection/index.js:30` already sets `id={title.toLowerCase()}` unconditionally (the `id="false"` bug is fixed at source) and `constants/products.js` is deleted outright, which took Ego and the other retired entries with it. Two roadmap items written from the pre-fix audit were removed, and the open question about which dictionary entries were retired is moot
- Both decisions were mirrored into the Gatsby repo's `.dev/roadmap.md`, so the plan of record does not contradict this one
