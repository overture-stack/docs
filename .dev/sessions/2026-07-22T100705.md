Initialized agentics at the project level, mirroring the lean prelude setup, since the developer's global context already carries all universal conventions.

- `AGENTS.md`: created as the canonical agent-neutral source; project-specific only (submodule/symlink content ownership, public-repo constraint, repo orientation), with the version tag `0.2.0 | synced: bcc6ee0`
- `CLAUDE.md`: created as a stub pointing at `AGENTS.md`
- `.dev/tech-debt.md`: created empty with the standard entry-format header
- `.dev/sessions/`: created with this file as the first session log (`.dev/roadmap.md` already existed)

Logged a build warning as tech-debt and upgraded Docusaurus after confirming no breaking changes between 3.9.2 and 3.10.2.

- `.dev/tech-debt.md`: added an entry for `image-size@2.0.2` failing to read `submission-system.svg` (draw.io export with an embedded `content` attribute that pushes the root `<svg>` tag past image-size's 1000-byte detection window). Confirmed non-fatal: `docusaurus build` still succeeds, just without explicit width/height on that one image.
- `website/package.json`, `website/package-lock.json`: bumped `@docusaurus/*` packages 3.9.2 → 3.10.2. Checked `facebook/docusaurus` GitHub releases for v3.10.0/3.10.1/3.10.2: no breaking changes, only opt-in feature flags and bug fixes. Verified with a full `docusaurus build` post-upgrade. First `npm i` attempt accidentally ran at the repo root (created a stray root `package.json`/`node_modules`) due to a shell cwd reset; reverted and redone correctly scoped to `website/` via `npm --prefix`.

Reviewed the full repo for cleanup opportunities and applied the changes the developer approved.

- `package-lock.json` (root): deleted; an orphaned empty lockfile with no corresponding `package.json`, traced to the `ce5c0195` "removed algolia for now" commit (Nov 2024), likely an accidental root-level `npm install` at the time.
- `docs` (root symlink): already removed from the filesystem independently before this pass; confirmed gone, no action needed.
- `website/casestudiesSidebars.ts`: deleted; unreferenced anywhere in the codebase, no matching plugin instance or content directory.
- `website/docusaurus.config.ts`: fixed the blog preset's `editUrl`, which still pointed at the unedited `create-docusaurus` starter-template path (`packages/create-docusaurus/templates/shared/`); now matches the docs preset's `editUrl` pattern (`https://github.com/overture-stack/docs`).
- `website/package.json`, `website/package-lock.json`: renamed the package from the stale placeholder `bridge-poc` to `overture-docs`, matching the project's actual name.
- Left alone by developer's choice: the dead `algolia:` themeConfig block and the unused `@docusaurus/theme-search-algolia` dependency (both orphaned since the same Nov 2024 removal, but not wired to anything — kept as-is for now).
- Flagged, not touched (submodule ownership, per `AGENTS.md`): `arranger`, `lectern`, `lyric`, `stage` all have uncommitted local modifications, and `lectern` has an untracked `docs/.DS_Store`; a broken symlink also exists inside the vendored `submodules/.github/standards/`. These are the submodules' own working-tree state, not this repo's to clean up.
- Verified with a full `docusaurus build` after each change; only the already-logged SVG `image-size` warning remains.

Reviewed the DRAC and ITCR grants and the arranger submodule's branches to plan grant-driven documentation work, then rewrote the roadmap.

- `.dev/roadmap.md`: replaced the single submodule-tracking item with a grant-driven documentation plan (DRAC as the 27-month binding driver at month 7; ITCR pending NOA so collaborator deliverables blocked). Four tiers: due-now/unblocked, ITCR-blocked, future DRAC objectives, housekeeping. Preserved and revised the submodule-tracking item.
- Arranger branch review (read-only; `git fetch` + `git show`/`ls-tree`, no checkout change): the Arranger MCP server docs the grants need already exist on arranger `main` (`docs/usage/06-ai-and-automation.md` plus SQON and introspection docs), but the site pins beta.15 (~158 commits behind `main`) so they never surface. MCP server code lives in `apps/mcp-server/` on `feat/1045-mcp-server-scaffold` and `poc/mcp-server-prompt-definition`, unmerged to `main`. Flagged the conflict: `06-ai-and-automation.md` is on `main` only, not `release` (`components-v3.0.7`), so the existing "track release not main" plan would hide the month-7 deliverables. Recorded as an open cross-repo decision in the roadmap.
- Grant source docs `DRACGrant.md`, `ITCRGrant.md` sit at repo root; confirmed they are untracked (not committed).

Made the grant documents durable, private, and cross-project per the single-source-of-truth model (moved out of the public repo).

- `~/.claude/grants/DRACGrant.md`, `~/.claude/grants/ITCRGrant.md`: canonical private copies, moved here from the docs repo root (repo root no longer carries them, removing the public-repo exposure risk).
- `~/.claude/projects/-Users-mshiell-Desktop-docs/memory/project-grants.md` + `MEMORY.md`: created the docs project's first memory entry (grant scope/interpretation, DRAC binding at ~month 7, ITCR pending NOA, capacity constraint, stay-within-scope + reporting guidance) and its index.
- `~/.claude/projects.md`: added a "Grants (cross-cutting context)" section so every project's session sees the two grants, the canonical location, and the scope/reporting working rule.
- `~/.claude/projects/-Users-mshiell-Desktop-prelude/memory/project_grants.md`: redirected its stale pointer (grants "at project root", but those files are gone from prelude) to the canonical `~/.claude/grants/` location, plus a cross-reference to `projects.md` § Grants. Surgical edit only; preserved prelude's own format and content. Confirmed no grant files remain anywhere in prelude.

Synced the agentics clone 0.2.0 → 0.7.0 and applied the two local gaps the upgrade check surfaced; the rest of 0.7.0 is picked up automatically since this project keeps every convention as a live pointer, not a local copy.

- `AGENTS.md`: version tag bumped to `0.7.0 | synced: ddfa748`
- `.dev/tech-debt.md`: replaced the restated entry-format header with a by-name reference to `session-discipline.md` § Tech-debt entry format; the format was duplicating what the convention already defines, which drifts the moment either copy changes
- `~/.claude/projects.md`: docs sync marker set to 0.7.0 / `ddfa748` (prelude still at 0.2.0 / `bcc6ee0`, to be upgraded in its own session)
- Diagnosis otherwise clean: no wrongly-created local `conventions/`/`CLAUDE.roles/`/`CLAUDE.softeng.md` copies, no leaked individual names in `.dev/`, no `agentics-overrides.md` needed

De-risked roadmap item A (surfacing the Arranger docs) with a read-only ground-truth pass plus a reversible trial build, then corrected the roadmap to match verified reality.

- `.dev/roadmap.md`: rewrote item A, the Key finding, and the submodule-tracking tension note. Verified: the target docs (SQON/introspection/MCP) exist on arranger `main` and on the `components-v3.1.0-rc.1` RC but not at the current pin (`c652992c`, 154 behind `main`) or the `release` branch; `apps/mcp-server` is already merged to `main` via PR #1065, so the roadmap's "unmerged package" blocker was stale; the autogenerated sidebar plus directory symlink surface new docs with no wiring changes
- Trial builds with the arranger pin temporarily advanced to `components-v3.1.0-rc.1` (restored to `c652992c` afterward, submodule left clean): compiles clean but fails one `onBrokenLinks: throw` check on arranger's `migration/v3.1.md`, whose two links point outside the symlinked `docs/` tree. Verified the docs-repo-side `migration/` exclude is **not** a viable stopgap: `overview.md` and `usage/07-defaults-and-limits.md` link *into* the migration doc, so excluding it just relocates the failure onto those inbound links. Item A is therefore gated on a two-line arranger PR making those upward links absolute (drafted at `scratchpad/arranger-migration-link-fix-PR.md`); everything else is verified green. No docs-repo changes committed this pass: the trial `docusaurus.config.ts` exclude and the pin bump were both reverted
- `.dev/roadmap.md`: item A updated — the in-repo stopgap does not work and the fix is upstream in arranger; the two-line `migration/v3.1.md` link fix was proposed to the arranger team to land before the 3.1.0 final release, and item A is marked in-flight pending that tag (then a verified-green pin bump)

Migrated the deprecated `onBrokenMarkdownLinks` Docusaurus config option ahead of its v4 removal.

- `website/docusaurus.config.ts`: moved `onBrokenMarkdownLinks: "warn"` from the top level into `markdown.hooks` (its v4 location); `onBrokenLinks: "throw"` stays top-level and is unaffected. Verified against the installed 3.10.2 config schema and a green build with the deprecation warning gone

Fixed the `image-size` build warning on Lectern's `submission-system.svg` (the standing tech-debt item); build output is now fully clean.

- `submodules/lectern/docs/assets/submission-system.svg`: stripped the embedded draw.io `content` attribute from the root `<svg>` tag (5.0MB → 1.26MB), so the tag closes within `image-size`'s 1000-byte SVG-detection window; `width`/`height`/`viewBox` and the rendered diagram are unchanged. Lectern submodule content, so it lands via lectern's own repo, then a docs pin bump; not committed in this repo. `.dev/tech-debt.md` entry updated to in-flight

Added the DRAC funding acknowledgement across the site (Tier 4); held ITCR pending its NOA.

- `website/community/02-funding.md`: added a Digital Research Alliance of Canada section (period 2026–2028; no award number provided yet) and refreshed the Last updated date
- `website/src/components/FundingStatment/index.tsx`, `website/src/components/SiteMap/index.tsx`: added DRAC to the "Supported by" statement and the "Funding Contributors" description
- ITCR/MSigDB deliberately not published — pending Notice of Award; a ready-to-activate entry is parked in `.dev/roadmap.md` Tier 4. Build verified: components compile and DRAC renders on the funding page (the build's broken-link failure is the arranger RC preview checkout, unrelated to this work)

Corrected an over-scope in roadmap item E (WCAG accessibility).

- `.dev/roadmap.md`: re-scoped item E to the conversational interface only, matching the DRAC deliverable ("WCAG 2.1 AA-compliant conversational interface with accessibility audit results"); removed the invented docs-site audit and org-level `.github/standards` accessibility standard as out of committed scope, and marked E blocked on item C (the interface is not built yet)

Restructured the Google-Docs CDD export into per-page markdown files (source material for the item C conversational-discovery docs).

- `CCDdoc/`: split the single 2196-line export into 26 per-page files across six themed subfolders (meetings, aim-1, aim-2, onboarding, grants, progress-reports), one file per Google-Doc page. Copied the 45-file `images/` folder in and converted the reference-style `![][imageN]` links to `../images/imageN.ext`, dropping the incomplete base64 data-URI blobs. Staging/reference area at repo root, outside the Docusaurus `website/` build. Source `CDD Context/` left intact

Refined the roadmap against the latest DRAC progress report (CCDdoc months 4-6), grounding it in real project status.

- `.dev/roadmap.md`: added a project-status anchor (Aim 1 on track for Month 8 sign-off; REST API, first MCP server, testing environment, and developer docs done or in hand). Corrected the tiering — the host application and MCP client are deferred to Aim 2 (months 8-11), so items C and E are Aim 2 work, not month-7; retitled Tier 1 and marked A/B/D as the actionable-now surfacing tasks. Updated item D to reflect the testing environment is already built (Docker + dev env + ARGO/Drug Discovery sample data; fixtures in progress), making it a surface-and-consolidate task with source material in `CCDdoc/`
- `.dev/roadmap.md`, `~/.claude/projects/.../memory/project-grants.md`: added a "function over implementation" scope principle — grant fulfilment is about the conversational-discovery function and end-user benefit, not named tools (Chainlit/LM Studio are reference implementations, not binding). Reworded items C and E to be host-agnostic and recorded the principle in project memory for future sessions

Restructured the roadmap around grant-independent operating principles, so decisions survive the grant.

- `.dev/roadmap.md`: added an "Operating principles" section at the top (single source of truth / author-in-source-repo, thin hub / no new machinery, deliberate pins not auto-follow-main, no transitive re-vendors, released-vs-development separation, in-repo Markdown not Google-Docs exports, plus function-over-implementation); reframed the grant tiers as time-boxed priorities within that model. Added a Tier 1 "vendor prelude" item as the first application (prelude is the adoption on-ramp and already the structured source for the CDD and testing-environment docs), and re-sourced items C and D from prelude (`[submodule]`) instead of authoring `[here]`, noting `CCDdoc/` is superseded staging. Renamed the submodule section to "Aggregation & pin governance" and added a no-transitive-re-vendors rule (exclude `prelude/docs/arranger`, which would double-surface arranger)

Reviewed the whole docs site and corrected the prelude approach: a project is not a reusable component, so it is not vendored wholesale.

- `.dev/roadmap.md`: sharpened principle 1 — the hub surfaces reusable-**component** reference docs (aggregated) and cross-cutting **guides** (authored here); a **project's** internal dev docs (benchmarks, evaluation, analytics, testing harness) stay in that project's repo and are not vendored. **Retracted the "vendor prelude wholesale" plan**: prelude is a project, so it stays a curated **Platform Tools** entry (`02-platform-tools/`, already the correct home — not drift), refreshed from prelude's user/admin docs and linked, with no submodule or pin decision. Rewrote item C as a cross-cutting Conversational Data Discovery **guide authored `[here]`** (informed by, not vendored from, prelude/`CCDdoc/`) and item D as the prelude Platform Tools quickstart refresh. Added a "Practical next steps" block mapping grant aims to the site: A (component docs, in flight) → C guide (now) → D quickstart (now); prelude project docs stay in prelude. Site review confirmed the four audiences (Guides = operators; Docs = component developers + Platform Tools; Workshop; Community)

Produced a site-wide review and restructuring proposal (a global view for maintainability + user simplicity), keyed to a multi-audience signposted structure.

- `.dev/docs-site-review.md`: created. Findings across maintainability (drifted `symlinker.sh`, no shared component-doc template, ungoverned pins, ad hoc core-vs-under-development classification with Lectern double-listed, vendored cruft, typo'd links) and user simplicity (no role-signposted entry, ambiguous Guides-vs-Documentation split, uneven component nav, "under development" encoding status as location, no per-audience "start here"). Recommendations given as quick-win vs bolder-target-state per theme (entry/signposting, component-doc consistency, guides boundary, classification, aggregation mechanics, content hygiene), a small-team sequencing, and the structural decisions left to the developer. Separate file from `.dev/roadmap.md` as requested; chosen items fold into the roadmap once picked

Applied the five sequenced docs-site quick wins and verified a green build; corrected the review with two discoveries.

- `symlinker.sh`: rewritten to reproduce the committed symlinks exactly (repo-root-runnable, idempotent, warns it must match committed reality) — closes the drift trap where regenerating rebuilt a different site
- `website/docs/03-under-development/03-under-development.md`: reframed so Lectern (a core component) is no longer double-listed as under-development; fixed the `overtue` typo, a Markdown indent bug (Maestro rendered as a code block), the wrong Lyric link, and prose typos
- `website/src/pages/index.tsx`: multi-audience meta description; removed dead commented buttons + the unused import
- `website/src/components/SiteMap/index.tsx`: category descriptions re-cast by audience/intent (operators / developers / try-locally); fixed the wrong Lyric link and a missing leading slash; removed a commented dead card
- `website/docs/00-getting-started.mdx`: `/docs` landing now signposts the operator (Guides) and newcomer (Workshop) audiences and cross-links; fixed a typo and a mislabeled card
- `.dev/docs-site-review.md`: corrected M2/recommendation B — the component-doc template **already exists** in `.github/standards/02-Software/02-Docs/`, so the gap is conformance not definition; added M7 — the funding commit `c466f878` accidentally committed a build-breaking arranger RC pin (no CI build gate caught it); added a status block
- Arranger pin: found the committed gitlink was the RC tag `1ab989d5` (broken migration links → committed repo did not build); set the working tree back to `c652992c` (item A's correct pin until the PR merges) — needs committing as a pin revert

Committed the chosen audience-journey IA (Deploy · Build · Use) to the roadmap as the target structure.

- `.dev/roadmap.md`: added a "Site structure — audience-journey IA (Deploy · Build · Use)" section — the three journeys with audiences and content mapping, Reference (component library) + Community as supporting areas, component status as a badge (retiring the "Under Development" section), how the grant quick wins (A→Reference, C→Use, D→Deploy) slot in, and a phased low-risk migration (authored content first, redirects for old routes, symlinker/sidebar reconcile last). Bundles the review's A/C/D bolder bets; respects thin-hub (nav reorg + one redirect plugin, not a rebuild) and single-source (Reference aggregation unchanged)
- `.dev/docs-site-review.md`: §5 updated — A/C (journey nav) and D (status badges) marked CHOSEN and folded into the roadmap; B (enforced template check) still open

Drafted the Phase-2 IA migration spec (nav scaffolding + content-move map) for review before executing.

- `.dev/ia-migration.md`: created. Target nav (Deploy · Build · Use · Reference · Community) mapped to Docusaurus `plugin-content-docs` instances (mirroring the existing guides/workshop/community pattern; Reference = the existing `docs` instance relabeled, keeping the `/docs` route to avoid mass redirects). File-by-file content-move map (guides → Deploy/Use, platform-tools → Deploy, api-reference/standards → Build, workshop + new CDD guide → Use), a redirect map (needs `@docusaurus/plugin-client-redirects`, not yet installed), four open sub-decisions with recommended defaults, and six ordered build steps. Phase 3 (under-development → Reference with status badges) kept separate. Draft only — nothing applied
- `.dev/roadmap.md`: added a pointer from § Site structure Phase 2 to the new spec

Decided to keep the `.dev/` planning docs separate (not merge into the roadmap) and made the split navigable.

- `.dev/roadmap.md`: added a "Working documents" map at the top (roadmap = durable plan / entry point; `docs-site-review.md` = findings + rationale; `ia-migration.md` = Phase-2 execution spec; tech-debt; sessions). Rationale: different document types and lifecycles; merging would bloat the session-start entry point
