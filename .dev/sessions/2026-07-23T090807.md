Surfaced the Arranger MCP / SQON / introspection developer docs on the site (roadmap item A) by advancing the arranger submodule pin, now that the upstream migration-link fix that was breaking the build under `onBrokenLinks: "throw"` has merged.

- `submodules/arranger`: pin advanced to `57ec8fb5` — `components-v3.1.0-rc.1` plus only the single migration-link doc fix (one commit ahead of the RC, not a divergence toward `main`). The `usage/03`–`08` and `concepts.md` docs, including `06-ai-and-automation.md` (the MCP server entry doc), now render on the site; a full build passes green. This supersedes the uncommitted pin-revert left after `c466f878` committed a build-breaking RC pin. Interim: re-point onto a release tag once one containing the fix is cut.
- `.dev/roadmap.md`: item A marked done and item B unblocked (the introspection doc is now on the site); the practical-next-steps entry and the Aggregation & pin-governance tension note updated to record the interim pin and the follow-up to re-pin onto a release tag.

Upgraded the project's agentics adoption 0.7.0 → 0.9.0, picking up the Overture product-family convention layer.

- `AGENTS.md`: version tag bumped to `0.9.0 | synced: b37cb60`; the Overture product-family layer (`CLAUDE.overture.md`) now listed alongside the softeng layer as applied-from-global-context, not duplicated here
- project memory (`overture-project.md`, `MEMORY.md`) and `~/.claude/projects.md`: recorded the Overture-project flag so future sessions load `CLAUDE.overture.md`. Its operative rule now governs the migration: links from any `docs/` file to a path outside the `docs/` subtree must be absolute `github.com/overture-stack/...` URLs, since the published site only symlinks `docs/` (relative up-and-out links 404); links between two files both inside `docs/` stay relative
- 0.8.0/0.9.0 convention changes (say-it-once density, re-verify `.dev/` entries before commit, dependency `dist-tags` check, cross-link-don't-duplicate) are live pointers, picked up automatically with no local copies to change

Executed the Deploy·Build·Use audience-journey IA migration (roadmap § Site structure, Phase 2): the site now presents intent-named journeys instead of the Guides/Documentation split. Full build green under `onBrokenLinks: "throw"`; committed in build-green steps.

- `website/docusaurus.config.ts`, `website/{deploy,build-docs,use}/`, `website/{deploy,build,use}Sidebars.ts`: new Deploy · Build · Use `plugin-content-docs` instances with authored `slug: /` landings; the `guides` and `workshop` instances are dissolved (Workshop folded under `/use/workshop`), and "Documentation" is relabeled "Reference" (route unchanged at `/docs`). The Build source dir is `build-docs/` to avoid colliding with Docusaurus's `build/` output dir
- authored guide content relocated into the journeys (deployment/administration + Prelude/quickstart → Deploy; api-reference/contributing/documentation-standards → Build; CLI guides + workshop → Use); per-journey `images/` dirs (the two differing `platform.svg` files disambiguated); intra-doc, Reference-landing, and homepage links re-pointed; `symlinker.sh` regenerates the standards symlink under `build-docs/`. Also fixed a pre-existing broken markdown link (Keycloak server docs) in the authorization guide while moving it
- `@docusaurus/plugin-client-redirects` added: every old `/guides/*`, `/docs/platform-tools/*`, `/docs/contribution`, `/docs/documentation-standards`, and `/workshop/*` URL redirects to its new home
- `website/remark/unlink-legacy-paths.js`: build-time transform stripping dead `/guides` links from vendored component docs (Song, Maestro), because redirect routes don't satisfy `onBrokenLinks: "throw"` for internal links and this repo can't edit vendored source. Interim; durable upstream fix (cross-link PRs + pin bumps) tracked in `tech-debt.md`
- `website/src/components/SiteMap/index.tsx`: homepage reorganized around the journeys (Deploy · Build · Use · Component Reference · Under Development)
- `.dev/ia-migration.md`, `.dev/roadmap.md`, `.dev/tech-debt.md`: spec marked executed with as-built deviations; roadmap Phase 2 marked done and the four sub-decisions resolved to defaults; tech-debt entries added for the upstream vendored-link fix and a personal-email-in-workshop flag

Folded the component Reference into the Build journey, revising the earlier standalone-at-`/docs` decision: the component library is all developer/integrator material, so it belongs in Build.

- `website/build-docs/01-core-software/`, `website/build-docs/05-under-development/`: moved whole (from the retired `docs` instance) so internal relative links stay intact; `docs: false` disables the default classic-preset docs instance and retires the `/docs` route; `symlinker.sh` regenerates the six component symlinks under `build-docs/` (same relative target, same depth)
- `website/docusaurus.config.ts`: dropped the standalone "Reference" navbar item (Reference is now inside Build); `createRedirects` maps every `/docs/core-software/*` and `/docs/under-development/*` URL to its `/build/*` home, plus `/docs` and `/docs/getting-started` → `/build`; footer "Reference" → `/build/core-software`; removed the now-unused `sidebars.ts`
- authored links repointed `/docs/core-software/` → `/build/core-software/` and `/docs/under-development/` → `/build/under-development/` (homepage `SiteMap`, workshop pages, the moved under-development docs, one admin page); Build landing refreshed to name the component library it now hosts
- tradeoff accepted: URL churn on the most-linked content, absorbed by redirects. Flagged a pre-existing component-link case-inconsistency (lowercase links vs capitalized routes) as standalone tech-debt

Redesigned the homepage so the Component Reference reads as part of Build, and rebalanced the layout.

- `website/src/components/SiteMap/index.tsx`, `styles.module.css`: the Build section now renders its guides plus a divided "Component Reference" sub-group (the six component cards) instead of Reference being a standalone category; mosaic rebalanced (Build left, Deploy + Use right) to remove the empty-column gap. Component links corrected to their capitalized routes (`/build/core-software/Lectern/overview`, etc.), resolving the homepage half of the case-inconsistency tech-debt (workshop links still pending)

Set Lato as the base font and finished normalizing the component links.

- `website/docusaurus.config.ts`, `website/src/css/custom.css`: load Lato (the Overture brand typeface, matching overture.bio) via a Google Fonts stylesheet + preconnect and set it as `--ifm-font-family-base`; the site had no explicit font and was falling back to the system stack
- `website/src/components/SiteMap/index.tsx`, `styles.module.css`: reordered the Build section so the component cards come before the API-reference/standards/contributing guides, and removed the "Component Reference" sub-heading
- `website/use/03-workshop/*.md`: capitalized the component `overview` links (Arranger/Stage/Maestro) and repointed the Lyric link to `/build/under-development/Lyric`, closing the component-link case mismatch. Remaining: four stale `arranger-components` cross-references (the page doesn't exist in the current Arranger docs) flagged in `tech-debt.md`

Cleaned up and compressed `.dev` now that the migration is executed.

- `.dev/docs-site-review.md`, `.dev/ia-migration.md`: deleted — both fully executed (git preserves the detail). Still-open items folded forward: the CI build-gate gap and the component-doc-shape-check decision → `tech-debt.md`; the migration's as-built specifics live in code comments + git
- `.dev/roadmap.md`: 223 → 116 lines — collapsed the finished item A and the Site-structure/Migration walkthrough to short done + Phase-3 notes, tightened the grant-status blocks, updated the Working-documents map; operating principles, open Tier items, and aggregation governance preserved
- `.dev/tech-debt.md`: added the CI-build-gate and docs-shape-check entries

Item B — Arranger API reference: authored the missing GraphQL query reference in arranger and wired the docs-site API-reference page to it.

- `submodules/arranger` (branch `overtureMCPdocsUpdate`, commit `bc15e4c8`): new `docs/usage/graphql-api.md` — the `POST /graphql` endpoint, an example query + response, the `hits`/`aggregations` schema (sourced from the arranger `graphql-router` source, not memory: `Connection`/`Edge`/`Node`, the `Sort` input `{fieldName, order, mode, missing}`, `Bucket {key, doc_count}`), the dot→`__` field-name convention, and schema discovery via the Introspection API; cross-linked from Query Processing. The gap: arranger documented SQON, introspection, and the query flow but had no reference for the GraphQL query API itself
- `website/build-docs/02-api-reference.mdx`: dropped the "Arranger under development" note, added an "Arranger GraphQL API" section linking to the arranger reference pages present at the current pin (query processing, SQON, introspection, MCP), and fixed the intro that promised four APIs but listed three
- docs pin left at `57ec8fb5` (the new arranger commit isn't on arranger's remote yet). Follow-up: push + PR the arranger branch, bump the pin to a merged commit, then link the dedicated `graphql-api.md` directly from the API-reference page

Items C and D — the Conversational Data Discovery guide and the Prelude entry.

- `website/use/conversational-data-discovery.md` (new, Use journey): the CDD guide — the function (natural language in, structured query out), how a question becomes a query (SQON filter + GraphQL field selection + aggregation), the four governing principles, the researcher workflow, connecting a client (the Arranger MCP server via MCP Inspector / LM Studio), and a "Try it locally" walkthrough of the CDD/MCP prelude demo. Function-first (survives the Aim-2 host choice); grounded in prelude's user docs and arranger's AI-and-automation reference
- `website/use/03-workshop/02-Architecture.md`, `09-Next-Steps.md`: retired the "coming soon (2026-2028)" CDD teaser, pointing to the new guide
- `website/deploy/01-prelude.md`: corrected a misread mid-task — prelude is the general locally-deployable demo-environment toolkit and the CDD/MCP environment is one specific demo of it (not a pivot), so the general Platform-Tools entry stays as-is with a prominent `overture-stack/prelude` repo link added, rather than being narrowed to the CDD demo
